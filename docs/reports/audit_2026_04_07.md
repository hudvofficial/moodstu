# Audit Report - 2026-04-07

## Summary

- 🔴 Critical Issues: 1
- 🟡 Warnings: 2
- 🟢 Suggestions: 1

## 🔴 Critical Issues (Phải sửa ngay)

1. **Vi phạm chuẩn Design System SSOT (Hardcode CSS rải rác)**
   - **Phạm vi**: Tìm thấy **>150 vị trí** ghi cứng mã màu (`#hex`, `rgba()`) khắp nơi trong thư mục `components/` (ví dụ: `status-select.tsx`, `offline-indicator.tsx`, `date-picker.tsx`, `gallery/...`).
   - **Nguy hiểm**:
     - Mất hoàn toàn "Single Source of Truth".
     - Không thể tự động thay đổi theme (Light/Dark mode) hoặc áp dụng bảng màu mới.
     - Sau này muốn đổi 1 màu (như màu "chính" hoặc màu "cảnh báo"), dev sẽ phải mở tay từng file ra tìm và thay thế, dẫn tới rủi ro sót file.
   - **Cách sửa**: Mở kế hoạch tái cấu trúc (Refactor CSS), gom hết màu về `globals.css` và thay thế toàn bộ mã cứng bằng `var(--color-...)` hoặc chuẩn Tailwind.

## 🟡 Warnings (Nên sửa)

1. **Lỗi Type Checking nội bộ (TypeScript Compile Error)**
   - **Phạm vi**: Code base (`npx tsc --noEmit` báo lỗi Exit 1).
   - **Nguy hiểm**: Cảnh báo ngầm rằng một số Component đang ghép sai dữ liệu, sai type. Lâu dần sẽ gây crash dở khóc dở cười lúc khách hàng nhấn nút mà không truy xuất được trường/properties.
   - **Cách sửa**: Bật chế độ soát Type và dọn dẹp các lỗi đỏ TypeScript cũ.

2. **Gói phụ thuộc bị cảnh báo bảo mật (NPM Dependencies)**
   - **Phạm vi**: 10 lỗi (8 High, 2 Moderate) chủ yếu đến từ các gói xây dựng PWA (`@ducanh2912/next-pwa`, `workbox-build`).
   - **Nguy hiểm**: Không gây chết lúc chạy (vì chỉ là gói lúc Build app), nhưng các bản build thư viện ẩn chứa rủi ro bị khai thác nếu Server / máy Builder bị dính shell script.
   - **Cách sửa**: Nâng cấp module tương ứng hoặc chạy `npm audit fix`.

## 🟢 Suggestions (Tùy chọn)

1. Quy hoạch lại file `utilities.css`.
   - Tailwind v4 đang quản lý style rất nghiêm, cần gom dọn lại bộ custom utils (các class manual như `.text-text-weekend`) để tránh rác sinh ra mỗi khi refactor.

## Next Steps

1. Mở `/plan` để lên cấu trúc đợt dọn rác SSOT.
2. Thiết lập quy chuẩn không bao giờ gõ chữ `#` trực tiếp trong file React `.tsx`.
