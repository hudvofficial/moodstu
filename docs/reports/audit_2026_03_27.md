# 🏥 Audit Report Round 4 — 2026-03-27

## Summary
- 🔴 Critical Issues: **1**
- 🟡 Warnings: **3**
- 🟢 Suggestions: **5** (DB housekeeping)
- ✅ Clean: inventory_items refactor hoàn tất 100%

---

## 🔴 Critical Issues (Phải sửa ngay)

### C1. `database.types.ts` chưa được cập nhật
- **File:** `types/database.types.ts`
- **Triệu chứng:** Vẫn chứa `inventory_items` view (line 1231) + 2 stale FK refs (line 281, 1357)
- **Nguy hiểm:** Types đã gen mới thành công (confirmed `dresses` table, no `inventory_items` view) nhưng **chưa được save vào file**. Cần overwrite file `database.types.ts` bằng output mới.
- **Cách sửa:** Chạy `npx supabase gen types` > lưu vào `types/database.types.ts`

---

## 🟡 Warnings (Nên sửa)

### W1. Dead file: `inventory-query-actions.ts`
- **File:** `app/actions/inventory-query-actions.ts` (79 dòng)
- **Triệu chứng:** File tồn tại nhưng **0 import** từ bất kỳ file nào
- **Nội dung:** 2 functions (`getItemTransactions`, `getAllTransactions`) cho transaction history
- **Cách sửa:** Quyết định giữ hoặc xóa. Nếu cần, migrate vào file phù hợp (e.g. `dress-queries.ts`)

### W2. 4 TODOs bị bỏ quên
| File | TODO |
|------|------|
| `app/actions/analyze-finance-actions.ts:9` | Port financeIntelligence, cashflowForecast |
| `app/actions/analyze-finance-actions.ts:20` | Port from V1 when lib/analytics/ available |
| `app/(protected)/layout.tsx:19` | [PRODUCTION] Khi implement multi-user |
| `app/(protected)/contracts/page.tsx:5` | Phase A+ — Migrate to server-side fetch |

### W3. Auth: Leaked Password Protection Disabled
- **Triệu chứng:** Supabase Auth chưa bật kiểm tra mật khẩu bị rò rỉ (HaveIBeenPwned)
- **Cách sửa:** Bật tại Dashboard > Auth > Settings > Password Security

---

## 🟢 Suggestions (Tùy chọn - DB Housekeeping)

### S1. RLS Policies: 5 tables có RLS bật nhưng chưa có policy
`gallery_albums`, `gallery_comments`, `gallery_reactions`, `lab_payments`, `salary_adjustments`

### S2. RLS Policies: 17 tables dùng `USING (true)` — quá permissive
Bao gồm: `addon_history`, `budgets`, `checklist_templates`, `credit_cards`, `financial_goals`, `goal_contributions`, `integrity_reports`, `inventory_transactions`, `investment_maintenance_logs`, `investments`, `login_attempts`, `notification_preferences`, `notification_queue`, `price_rules`, `receipts`, `service_relations`

> **Đánh giá:** Đây là app internal (không public), chỉ authenticated users. Mức rủi ro **THẤP** cho single-tenant studio. Tuy nhiên nếu mở rộng multi-tenant thì cần tighten.

### S3. 6 Functions thiếu `search_path` setting
`get_employee_productivity`, `get_employee_job_details`, `contribute_to_goal`, `decrement_goal_amount`, `cancel_contract_cascade`, `delete_contract_cascade`

### S4. ~47 Unindexed Foreign Keys + ~48 Unused Indexes
- **INFO level** — phần lớn là `created_by` FKs (hiếm khi query by)
- Một số đáng chú ý: `contract_items_inventory_item_id_fkey`, `contract_items_service_id_fkey`
- Có **1 duplicate index**: `galleries` có `idx_galleries_contract` + `idx_galleries_contract_id` (trùng)

### S5. RLS Performance: 5 policies cần `(select auth.fn())` wrapper
Tables: `employees`, `dress_rentals` (4 policies), `dress_rental_accessories`

---

## ✅ Clean Checks (Passed)

| Check | Result |
|-------|--------|
| `inventory_items` trong `app/` | ✅ 0 hits |
| `inventory_items` trong `components/` | ✅ 0 hits |
| `inventory_items` trong `lib/` | ✅ 0 hits |
| `inventory-actions` imports | ✅ 0 hits (file deleted) |
| Hardcoded secrets | ✅ 0 hits |
| `npm run build` | ✅ EXIT CODE 0 |
| TODOs trong `components/` | ✅ 0 hits |

---

## 📋 Next Steps

```
1️⃣ Fix C1: Save database.types.ts mới
2️⃣ Fix W1: Xóa inventory-query-actions.ts (dead file)
3️⃣ Fix W3: Bật Leaked Password Protection
4️⃣ Fix S4: Drop duplicate index trên galleries
5️⃣ Bỏ qua S1-S3 (low risk cho single-tenant)
```
