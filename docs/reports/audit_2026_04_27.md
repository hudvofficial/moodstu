# Audit Report - 2026-04-27 (Login Performance Focus)

## Summary
- 🔴 Critical Issues: 0
- 🟡 Warnings: 1
- 🟢 Suggestions: 1

## 🟡 Warnings (Nên sửa)
1. Dư thừa một bước gọi Database làm chậm quá trình đăng nhập
   - File: `app/actions/auth.ts`
   - Nguyên nhân: Hiện tại, mỗi khi anh đăng nhập thành công, hệ thống **luôn luôn** gửi một lệnh "Xóa lịch sử nhập sai" xuống cơ sở dữ liệu, ngay cả khi trước đó anh chưa từng nhập sai lần nào.
   - Hậu quả: Làm tốn thêm 1 nhịp chờ mạng (khoảng 100-200ms) không cần thiết.
   - Cách sửa: Chỉ thực hiện xóa nếu trước đó hệ thống ghi nhận có việc nhập sai.

## 🟢 Suggestions (Tùy chọn)
1. Tối ưu lệnh kiểm tra đăng nhập sai
   - File: `app/actions/auth.ts`
   - Nguyên nhân: Hàm kiểm tra đang dùng `.single()`, hàm này sẽ tạo ra một lỗi ngầm (error) nếu không tìm thấy dữ liệu (người dùng chưa nhập sai bao giờ). Xử lý lỗi ngầm này làm chậm hệ thống một chút xíu.
   - Cách sửa: Chuyển sang dùng `.maybeSingle()`, báo cho hệ thống biết "không có cũng không sao", trả về rỗng luôn cho nhanh.

## Next Steps
Áp dụng cả 2 sửa đổi này sẽ giúp giảm thời gian đăng nhập đi khoảng 1/3 (cắt được 1 chuyến đi vòng qua database).
