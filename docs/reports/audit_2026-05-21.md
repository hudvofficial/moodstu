# Audit Report - 2026-05-21

## Summary
- 🔴 Critical Issues: 2
- 🟡 Warnings: 3
- 🟢 Suggestions: 2

## 🔴 Critical Issues (Phải sửa ngay)
1. **Lỗ hổng bảo mật thư viện (High Severity)**
   - File: `package.json` / `node_modules`
   - Nguy hiểm: `npm audit` phát hiện 15 lỗ hổng (11 High, 4 Moderate), bao gồm thư viện `next`, `dompurify` và `picomatch` có khả năng bị tấn công XSS hoặc từ chối dịch vụ (DoS).
   - Cách sửa: Chạy `npm audit fix` hoặc cập nhật phiên bản các thư viện bị lỗi.

2. **Vi phạm Design System (SSOT Violation)**
   - File: `gallery-list-modal.tsx`, `event-form-drawer.tsx`, và nhiều nơi khác.
   - Nguy hiểm: Dùng thẻ `<input>` thuần của HTML thay vì component `<Input>` chuẩn của `@/components/ui`. Điều này làm vỡ layout thống nhất, mất đồng bộ UI/UX và có thể gây lỗi về styling hoặc validation.
   - Cách sửa: Thay thế toàn bộ `<input>` thành `<Input>` import từ `ui`.

## 🟡 Warnings (Nên sửa)
1. **Lỗi Code Quality (Linting)**
   - Nguy hiểm: Hiện có tổng cộng 55 errors và 33 warnings từ ESLint (nhiều biến `any`, unused variables, thiếu dependency array trong hook `useEffect`/`useMemo` ở `use-masonry-grid.ts` và `gallery-toolbar.tsx`). Code dễ bị bug tiềm ẩn khi scale lên.
   - Cách sửa: Refactor và clean up các biến không dùng, bổ sung dependencies vào các React hooks.

2. **Component quá khổ (Performance & Maintainability)**
   - File: `event-form-drawer.tsx` (~28KB), `stock-out-modal.tsx` (~25KB), `contract-detail-client.tsx` (~24KB).
   - Vấn đề: Quá nhiều logic render trong một file duy nhất làm app chậm (re-render nặng), tải trang lâu và rất khó bảo trì.
   - Cách sửa: Cần tách nhỏ các component này thành nhiều component con (Sub-components) theo từng chức năng độc lập.

3. **Cảnh báo về File Rác**
   - File: Thư mục `tmp/` chứa các script `capture_shotpik.js`, `open_browser_for_login.js` dùng `require()` sai chuẩn TypeScript.
   - Vấn đề: Gây cảnh báo lộn xộn trong project.
   - Cách sửa: Có thể bỏ qua lint thư mục này bằng `.eslintignore`.

## 🟢 Suggestions (Tùy chọn)
1. **Tối ưu Fetching Database**
   - File: `app/actions/work-task-actions.ts`
   - Vấn đề: Đang gọi database bằng Supabase rải rác nhiều nơi (có thể bị N+1 nếu không cẩn thận). Nên gộp truy vấn hoặc dùng Edge functions để tối ưu tốc độ cho Production.

2. **Tối ưu Performance Audit Tool**
   - Script `perf:audit` đã chạy thành công nhưng hiện chưa ghi nhận block nào lớn. Có thể tích hợp thêm Lighthouse CI để đo lường tự động khi deploy.

## Next Steps
- Tham khảo ý kiến user để fix bug trước khi Deploy.
