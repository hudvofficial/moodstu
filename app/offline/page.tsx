import OfflinePageClient from "@/components/layout/offline-page-client";

export const metadata = { title: "Không có kết nối mạng" };

export default function OfflinePage() {
  return <OfflinePageClient />;
}
