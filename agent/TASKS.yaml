# TASKS.yaml — Hàng đợi việc + bảng khóa/quyền sở hữu
#
# LUẬT (xem agent/AGENT_RULES.md §3):
#   - Mỗi task 1 `owner` tại một thời điểm. Chỉ owner được ghi trong `locks`.
#   - Hai task chưa `merged` KHÔNG được có `locks` giao nhau.
#   - Đổi status/owner = đồng thời viết 1 file trong HANDOFFS/.
#
# status: spec | approved | implementing | testing | review | fixing | ci | merged | blocked
# owner theo status: spec/review/ci -> claude ; implementing/fixing -> codex ; testing -> roo
#
# ---- SCHEMA 1 task ----
# - id: T-YYYYMMDD-<slug>          # định danh duy nhất
#   title: <mô tả ngắn>
#   status: spec
#   owner: claude
#   branch: codex/<slug>           # branch/worktree Codex làm
#   worktree: .worktrees/<slug>    # (nếu dùng worktree riêng)
#   locks:                         # vùng độc quyền của task — KHÔNG chồng task khác
#     - components/<module>/**
#     - app/actions/<module>-*.ts
#   spec: agent/HANDOFFS/T-YYYYMMDD-<slug>.spec.md
#   verify:                        # gate phải xanh trước merge
#     - npm run build
#     - npm run verify:<module>
#   depends_on: []                 # id task phải merged trước
#   notes: <ghi chú>

version: 1
updated: 2026-07-16

# ---- Task đang chạy ----
active:
  - id: T-20260716-gallery-metadata-dedup
    title: "Cắt double-fetch metadata gallery: đường thành công dùng V3 + 1 query nạc commentsPerImage (bỏ 3/4 query metadata)"
    status: implementing
    owner: codex
    locks:
      - app/actions/gallery-composite-actions.ts        # chỉ THÊM getGalleryCommentContentAll
      - components/contracts/gallery/use-gallery-data.ts # chỉ sửa loadGalleryData
    spec: agent/HANDOFFS/T-20260716-gallery-metadata-dedup.spec.md
    verify:
      - npm run build
    depends_on: [T-20260716-gallery-notes-to-comments]
    notes: "Perf đo prod: admin gallery LCP 2.59s; mỗi lần đổi album chạy V2 RPC + getGalleryMetadataAll(4 query) song song, nhưng V3 RPC đã đọc gallery_comments nên 3/4 query metadata thừa. Chỉ commentsPerImage (nội dung) là V3 thiếu → lấy 1 query nạc. metadata GIỮ cho fallback. KHÔNG migration, KHÔNG prod DB. Rủi ro review: albums giờ từ V3 — verify tab+count khớp."

  - id: T-20260716-comment-identifier-leak
    title: "Vá lỗ khách sửa/xoá được ghi chú khách khác: getComments bỏ client_identifier, trả is_mine (server tính)"
    status: approved
    owner: claude
    locks:
      - app/actions/gallery-reaction-actions.ts   # chỉ interface GalleryComment + getComments
      - components/gallery/image-viewer.tsx        # chỉ chỗ dùng client_identifier
    spec: agent/HANDOFFS/T-20260716-comment-identifier-leak.spec.md
    verify:
      - npm run build
    depends_on: [T-20260716-gallery-notes-to-comments]
    notes: "Audit 16/07 P2 nhưng đụng feature d45ea00. getComments lộ client_identifier MỌI người → khách A ghi đè/xoá note khách B. Fix: server tính is_mine, không trả identifier; mutation dùng identifier của chính người gọi nên tự-scope. QUEUE SAU B (serialize Codex review, dù file không chồng B)."

# ---- Cần đối chiếu (in-flight ngoài quy trình, backfill quyền sở hữu nếu resume) ----
known_inflight: []

# ---- Đã bỏ (abandoned) ----
abandoned:
  - branch: codex/contract-optimization   # đã xoá
    worktree: .worktrees/contract          # đã gỡ
    area: contract module
    date: 2026-07-15
    recovery_tag: abandoned/contract-optimization-20260715   # git checkout tag này để khôi phục
    reason: "User quyết BỎ HẲN 2026-07-15. Việc dở (6 file source + migration reactivate_contract_atomic) snapshot vào WIP commit 2b838a7 dưới tag khôi phục — không mất."

# ---- Đã xong ----
done:
  - id: T-20260715-gallery-password-gating
    title: "Mật khẩu đúng nghiệp vụ ADR-008 (tim tự do, chọn+note cần pass) + fix CSS --spacing-* đè Tailwind"
    status: merged
    merged_commit: "dc9b1d9 + 5ffc756 (z-index/copy/center) + font pill"
    spec: agent/HANDOFFS/T-20260715-gallery-password-gating.spec.md
    result: "Claude code trực tiếp (user yêu cầu). Verify prod bằng đo computed style: max-w-sm 8px→384px, token cũ biến mất, z-10001 nổi trên lightbox. Modal copy tối giản + nút center + font-medium theo 2 vòng feedback user."
    notes: "ADR-008: view-token tự do / select-token sau pass; đã kiểm không có đường leo thang quyền view→select."

  - id: T-20260715-gallery-hearts-visibility
    title: "Tách bạch Chọn vs Tim: pill Chọn public + stat/filter/download tim thuần + heartCount admin"
    status: merged
    merged_commit: 4c8e0b5
    spec: agent/HANDOFFS/T-20260715-gallery-hearts-visibility.spec.md
    result: "Codex 9 file khớp spec 11/11; Claude review bắt 1 lỗi pill elip (fix borderRadius); eslint 0 lỗi + build pass + CI xanh; screenshot prod xác nhận tile heart hiện không hover; user xác nhận trên prod 15/07."
    notes: "Quy ước chốt: ✓ xanh = Chọn (is_selected) · ❤️ đỏ = Tim (gallery_reactions). CẤM gộp bằng ||."

  - id: T-20260715-image-viewer-lint
    title: "Làm sạch lint components/gallery/image-viewer.tsx (1 bug hook + 9 SSOT)"
    status: merged
    branch: codex/image-viewer-lint    # remote branch còn sống — có thể xoá
    merged_pr: 5
    merged_commit: 146f6e1             # Merge PR #5 → main (fix 675dced)
    spec: agent/HANDOFFS/T-20260715-image-viewer-lint.spec.md
    result: "Codex impl → Claude review bắt regression heart-fill → Codex fix → CI quality xanh → user merge. Merged: fill-error text-error, 0 native <button>, hook bug đã sửa."
    notes: "Dogfood pipeline 3-agent lần đầu — end-to-end OK. Review layer bắt được bug lint+build đều bỏ lọt."

  - id: T-20260716-gallery-notes-to-comments
    title: "Ghi chú khách: cột client_note (1/ảnh, admin không thấy) → bảng gallery_comments (upsert theo người, có tên)"
    status: merged
    merged_commit: d45ea00
    spec: agent/HANDOFFS/T-20260716-gallery-notes-to-comments.spec.md
    result: "Codex impl (3 vòng: chặn spec thiếu lock → review bắt 3 lỗi R1-R3 → fix) → Claude tự chạy migration 20260716000000 (backfill 148 note) + verify prod: 126 Bình luận (từ 0) + filter 126 ảnh + 31 chip + preview 'In lớn'. client_note giữ nguyên 148 dòng làm backup."
    notes: "Bug prod: 148 ghi chú in ấn của khách (HĐ-2026-0028: 126) vô hình với xưởng — admin đếm/lọc theo gallery_comments (0 dòng) còn khách ghi vào client_note. 4 action comment cũ HỞ BẢO MẬT (admin client, không gate) → task này siết: ghi = capability 'select' (mật khẩu), đọc = 'view'. User chốt: có hỏi tên (nhớ localStorage) + mỗi người 1 ghi chú/ảnh sửa được của mình. CÒN TỒN (audit 16/07): getComments public vẫn lộ client_identifier → khách sửa được note khách khác (P2); rate-limit verifyGalleryPassword (P1)."
