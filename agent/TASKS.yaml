# TASKS.yaml — Hàng đợi việc + bảng khóa/quyền sở hữu
#
# LUẬT (xem agent/AGENT_RULES.md §3):
#   - Mỗi task 1 `owner` tại một thời điểm. Chỉ owner được ghi trong `locks`.
#   - Hai task chưa `merged` KHÔNG được có `locks` giao nhau.
#   - Đổi status/owner = đồng thời viết 1 file trong HANDOFFS/.
#
# status: spec | approved | implementing | testing | review | fixing | ci | merged | blocked
# owner theo status: spec/review/ci -> claude ; implementing/fixing -> codex ; testing -> roo
#
# ---- SCHEMA 1 task ----
# - id: T-YYYYMMDD-<slug>          # định danh duy nhất
#   title: <mô tả ngắn>
#   status: spec
#   owner: claude
#   branch: codex/<slug>           # branch/worktree Codex làm
#   worktree: .worktrees/<slug>    # (nếu dùng worktree riêng)
#   locks:                         # vùng độc quyền của task — KHÔNG chồng task khác
#     - components/<module>/**
#     - app/actions/<module>-*.ts
#   spec: agent/HANDOFFS/T-YYYYMMDD-<slug>.spec.md
#   verify:                        # gate phải xanh trước merge
#     - npm run build
#     - npm run verify:<module>
#   depends_on: []                 # id task phải merged trước
#   notes: <ghi chú>

version: 1
updated: 2026-08-24

# ---- Task đang chạy ----
active:
  - id: T-20260723-gallery-dead-code-cleanup
    title: "Gỡ 5 file gallery chết — chuỗi Pinterest 3 file ĐANG nằm trong bundle gửi khách"
    status: spec
    owner: claude
    branch: codex/gallery-dead-code-cleanup
    locks:
      - scripts/test-url-helper.mjs
      - components/gallery/gallery-virtual-grid.tsx
      - components/contracts/gallery/gallery-image-grid-pinterest.tsx
      - components/contracts/gallery/use-masonry-pinterest.ts
      - components/contracts/gallery/gallery-image-grid-index.tsx
      - app/actions/gallery-masonry-layout.ts
    spec: agent/HANDOFFS/T-20260723-gallery-dead-code-cleanup.spec.md
    verify:
      - npx eslint components/contracts/gallery/gallery-image-grid-index.tsx
      - npm run build
    depends_on: []
    notes: "Gốc rễ: logic dựng URL ảnh có 3 bản sao ở 3 nơi → vá bẫy ?size ở 01a2ca8+1477090 vẫn còn 2 bản ngoài tầm vá; task này rút còn 1. ĐO ĐƯỢC (không suy đoán): chuỗi class riêng của Pinterest nằm trong .next/static/chunks/7260-0e8f77e05f9ea258.js (20 KB) dù USE_PINTEREST=false nên chưa từng chạy. Chuỗi chết: pinterest → use-masonry-pinterest → app/actions/gallery-masonry-layout, mỗi mắt xích chỉ có 1 nơi gọi là mắt xích trước. resolveThumbnailSize là bản CỤC BỘ trong chính 2 file bị xoá → không mồ côi ai. @tanstack/react-virtual còn 2 consumer khác → KHÔNG đụng lockfile. Rủi ro thật: mất đường lùi bật lại Pinterest (lấy lại từ git history nếu cần)."
  - id: T-20260723-worktree-salvage-prune
    title: "Cứu 2 thay đổi chưa commit trong worktree cũ, rồi mới gỡ (thu hồi 5.2 GB)"
    status: spec
    owner: claude
    branch: codex/worktree-salvage
    locks:
      - components/contracts/gallery/use-gallery-data.ts
      - components/gallery/public-gallery-client.tsx
    spec: agent/HANDOFFS/T-20260723-worktree-salvage-prune.spec.md
    verify:
      - npx eslint components/contracts/gallery/use-gallery-data.ts components/gallery/public-gallery-client.tsx
      - npm run build
    depends_on: []
    notes: "CỨU TRƯỚC, GỠ SAU — không được đảo. 2 worktree ở .claude/worktrees/ (2.3 GB + 2.9 GB) đều giữ thay đổi CHƯA COMMIT và CHƯA có trên main (grep toast.error trên main = 0 ở cả 2 file). Cả hai đều vá cùng một lớp bệnh: nuốt lỗi rồi trả rỗng khiến triệu chứng giống trạng thái bình thường (Download Manager '0 file'; lưu ghi chú fail im lặng). Task 3 (gỡ worktree) ĐỘNG VÀO MÔI TRƯỜNG MÁY USER → phải hỏi xác nhận riêng dù spec đã duyệt; worktree remove --force là mất vĩnh viễn phần chưa commit. Rủi ro: git apply có thể xung đột vì worktree đứng ở fcc71b6 còn main đã ở a521109 — spec bắt DỪNG chứ không tự chế."

# ---- Cần đối chiếu (in-flight ngoài quy trình, backfill quyền sở hữu nếu resume) ----
known_inflight: []

# ---- Đã bỏ (abandoned) ----
abandoned:
  - branch: codex/contract-optimization   # đã xoá
    worktree: .worktrees/contract          # đã gỡ
    area: contract module
    date: 2026-07-15
    recovery_tag: abandoned/contract-optimization-20260715   # git checkout tag này để khôi phục
    reason: "User quyết BỎ HẲN 2026-07-15. Việc dở (6 file source + migration reactivate_contract_atomic) snapshot vào WIP commit 2b838a7 dưới tag khôi phục — không mất."

# ---- Đã xong ----
done:
  - id: T-20260824-customer-search-contrast
    title: "Ô Tìm kiếm khách hàng ở form hợp đồng chìm trên nền trang (1.03:1) — thêm lớp SSOT input-elevated"
    status: merged
    owner: claude (fallback)
    branch: claude/customer-search-contrast
    locks:
      - components/contracts/form/ContractCustomerSection.tsx
      - app/styles/forms.css
    spec: agent/HANDOFFS/T-20260824-customer-search-contrast.spec.md
    verify:
      - npx eslint components/contracts/form/ContractCustomerSection.tsx
      - npm run build
      - "render chrome-devtools /contracts/create @375 @768 @1024 (Roo)"
    depends_on: []
    notes: "2026-08-24 VERIFY XANH (eslint 0 lỗi, build exit 0, render thật prod @375/768/1024 + focus, seed user E2E đã xoá) — chờ user merge branch claude/customer-search-contrast → main + push. Placeholder chốt là \"Tìm hoặc tạo mới...\" (đo 126px < 143px vùng chữ @375; bản dài 179px bị cắt). ĐO ĐƯỢC: section Khách hàng nằm trực tiếp trên nền trang (--color-bg-base #faf7f2), ô input-base (#f8f4ee, không border theo Lesson #64) → tương phản ~1.03:1; placeholder --color-text-muted ~2.1:1. Fix = lớp unlayered .input-elevated (bg card + inset ring bằng box-shadow + shadow-sm, placeholder text-secondary ~4.5:1) — cùng từ vựng với .search-input của header. Không đụng .input-base/.input-selected. Placeholder đổi chữ là TÙY CHỌN trong spec. Lock không giao với 2 task gallery đang active."
  - id: T-20260807-lightbox-instant-placeholder
    title: "Mở ảnh bị 'chớp' (khung đen 0,3–2,3s): hiện ngay ảnh lưới =s600 + hoãn nạp bản gốc =s0"
    status: merged
    merged_commit: 27f25a0
    spec: agent/HANDOFFS/T-20260807-lightbox-instant-placeholder.spec.md
    result: "User báo lỗi, hỏi lại xác nhận triệu chứng = màn đen rồi ảnh mới bung (không phải ảnh hiện rồi nháy). Claude tự code, user duyệt spec + chốt 2 lựa chọn (giữ =s0 nhưng hoãn; áp cả 2 file). Verify: eslint 0, build 0, và đo lại bằng đúng phương pháp đã tìm ra bug — admin @1440 dialog hiện 395ms + ảnh vẽ CÙNG frame (trống 0ms, trước: 334/1319/2337ms), khung giữ 540x810 suốt 3 nấc; khách @390 ảnh vẽ 221ms, request =s0 start 790ms > =s1200 end 777ms (hoãn đúng), chuyển ảnh next 0 frame trống; @768 thẻ 615x922 đúng tỉ lệ, không tràn ngang; screenshot @1440 + @390 mắt thấy ảnh ngay. CHƯA verify trên prod sau deploy."
    notes: "ĐO ĐƯỢC (prod, Chrome 1440): mỗi lần mở 1 ảnh tải SONG SONG =s2048 (450 KB, 2100ms) + =s0 (4730x7092, ~19,4 MB, 2507ms), cả hai start t≈106ms, trong khi khung chỉ 540x810 → comment cũ 'hiện bản nhẹ trước nên tốc độ mở không đổi' (commit 2e4ea54) SAI. Tải riêng =s2048 vẫn 1613ms → hoãn =s0 thôi không đủ, phải có ảnh chờ. Ảnh lưới =s600 dựng bằng getResponsiveThumbnailUrl(thumbnail_url, image_url, 600) — gọi ĐÚNG hàm đó mới trùng URL lưới và hit cache (đo: load 0ms). ĐÃ LOẠI TRỪ: không phải nháy do đổi src sang =s0 — theo dõi từng frame, chiều cao <img> giữ 810px, layout-shift rỗng, CLS 0.00. BẪY BẮT ĐƯỢC LÚC VERIFY: đặt ảnh chờ xong thì desktop hiện 400x600 rồi nở lên 540x810 (md:w-auto lấy kích thước nội tại) → phải thêm md:h-[90vh]; mobile không dính vì đã khoá w-[100vw]. Loader nền phải set CẢ srcset lẫn sizes giống thẻ thật, nếu không trình duyệt chọn candidate khác (mobile =s1200) → tải 2 lần. CÒN TỒN: vẫn tải ~19,4 MB/ảnh (user chốt giữ để chuột-phải/nhấn-giữ lưu ra file gốc); sizes='95vw' sai với ảnh dọc (khung thật 540px) — task riêng nếu muốn."
  - id: T-20260806-gallery-filter-mode
    title: "Modal Lọc ảnh: 3 radio là UI chết → chọn thật Khách chọn / Thả tim / Cả hai, áp cho cả 3 tab"
    status: merged
    merged_commit: 091d40b
    spec: agent/HANDOFFS/T-20260806-gallery-filter-mode.spec.md
    result: "User duyệt spec rồi yêu cầu Claude tự code, KHÔNG qua Codex. 9 task/5 file đúng spec. Verify: eslint 5 file exit 0; npm run build exit 0; render thật localhost:3000 gallery hoaikha-thihiu (163 khách chọn/159 tim) — đổi radio cho ra ĐÚNG 163 / 159 / 166, khớp ground truth query thẳng DB (chon_jpg_drive=163, tim=159, union=166, overlap=156); radio hiện + giữ lựa chọn ở CẢ 3 tab (checkedIndex không đổi khi chuyển tab); screenshot @1024 và @390 + đo @768 không tràn ngang, nút Bắt đầu lọc trong khung. Lặp lại ĐÚNG 3 nhánh query mới của initDriveCopyJob bằng supabase-js trên DB thật: selected=163 rows, hearted=159 (.in với 159 UUID chạy được, không lỗi URL/cú pháp), both=166 — khớp số client. CHƯA chạy copy Drive thật (tạo folder+shortcut trên Drive khách, khó lùi) → để user bấm 1 lần trên gallery test."
    notes: "Root cause đo được: Radio name='filterType' không state/không onChange và handleCopyDrive không đọc → 3 lựa chọn chỉ là trang trí. Server initDriveCopyJob tự query union(is_selected OR heart) bỏ qua danh sách client, còn Local/Export dùng fetchAllHeartedDownloadFiles (CHỈ tim) → (1) số hiển thị 159 lệch với union server copy ≥163, (2) 2 tab kia bỏ sót ảnh khách chọn không tim. Bug phụ bắt thêm: server không lọc drive_file_id nên ảnh thiếu id vào filesToCopy → fail chắc chắn + phồng total_count. Mặc định chốt = 'Ảnh khách chọn' (đổi hành vi mặc định cũ, user duyệt). 'Ảnh có tag' vẫn disabled vì gallery_images KHÔNG có cột tag (cột tags là của bảng galleries); 'Ảnh có bình luận' làm được (gallery_comments) nhưng ngoài phạm vi. BẪY GIỮ LẠI: query đếm bằng supabase-js bị PostgREST chặn 1000 dòng/request bất kể .limit() → lần đầu ra số sai (chọn 96/tim 112), phải phân trang .range() mới ra đúng 163/159."
  - id: T-20260806-sentry-sw-register-noise
    title: "Lọc noise Sentry 'Error: Rejected' — next-pwa gọi workbox.register() không .catch()"
    status: merged
    merged_commit: 9a549a9
    spec: agent/HANDOFFS/T-20260806-sentry-sw-register-noise.spec.md
    result: "User yêu cầu Claude viết spec rồi tự làm, KHÔNG qua Codex. Diff đúng 1 file, 1 phần tử mảng + comment, khớp spec. Verify đủ 3 bước: eslint instrumentation-client.ts exit 0; npm run build exit 0 (kèm PWA artifact check pass); chạy chính helper của Sentry (getPossibleEventMessages + stringMatchesSomePattern trong @sentry/core) để chứng minh /^Rejected$/ lọc đúng event 'Error: Rejected' mà KHÔNG lọc 'SecurityError: Failed to register a ServiceWorker...' lẫn 'Error: Payment Rejected by gateway'. Không xác nhận được ngay hiệu quả trên prod — phải xem số event mới của issue sau vài ngày."
    notes: "ĐO ĐƯỢC: source dự án không có chỗ nào gọi serviceWorker.register (3 chỗ chạm SW đều là getRegistration/update/ready và đã có .catch); nguồn duy nhất là next.config.ts:213 register:true → @ducanh2912/next-pwa/dist/sw-entry.js chạy window.workbox.register() trần → unhandledrejection. Sentry client init nằm ở instrumentation-client.ts, KHÔNG có file sentry.client.config.ts. SUY RA CHƯA ĐỐI CHỨNG: fbclid + frame <anonymous> ⇒ in-app browser Facebook/Messenger từ chối SW — cách vá không phụ thuộc giả thuyết này (lọc theo chữ ký lỗi). Tác động người dùng = 0: /gallery là trang public read, SW chỉ lo offline cache + push của PWA. Bẫy: ignoreErrors match SUBSTRING với pattern kiểu string → phải dùng regex neo 2 đầu. Đã loại phương án register:false + tự register có try/catch (phải viết lại wiring workbox-window của cả PWA, rủi ro treo build cũ)."
  - id: T-20260723-drive-proxy-size-trap
    title: "Gỡ 2 helper trả URL /api/drive-download thiếu ?size (bẫy nạp nguyên file gốc 15 MB làm thumbnail)"
    status: merged
    merged_commit: 1477090
    spec: agent/HANDOFFS/T-20260723-drive-proxy-size-trap.spec.md
    result: "User duyệt spec rồi yêu cầu Claude tự làm, KHÔNG qua Codex. Diff khớp spec đúng 2 task, 2 file, không dòng thừa (Claude tự git diff đối chiếu). Verify đủ 5 bước: eslint 0; build 0 (build là cổng TS — gọi sai hàm/thừa đối số sẽ đỏ); grep getDriveDownloadUrl rỗng; grep useProxy rỗng; render /gallery/amtgzexYOnXG @390x844x3 cho khong_co_size=0, 30/30 ô đã tải đều dùng =s600, 0 ô 'Lỗi nguồn Drive', ảnh chụp xác nhận lưới bình thường."
    notes: "Đóng nốt bẫy vá một nửa ở 01a2ca8. ?size là TUỲ CHỌN (cố ý — đường tải ảnh gốc phải giữ =s0), nên bẫy chỉ đóng hẳn khi không còn helper nào trả URL proxy trần. Xác minh dead trước khi xoá: getDriveDownloadUrl 0 nơi gọi toàn repo (grep nền quét cả worktree/scripts/tests xác nhận lại lần 2); useProxy có 3 caller nhưng cả 3 chỉ truyền 3 đối số. GIỮ getDriveThumbnailUrl + getDriveImageUrl (5 call site). CÒN TỒN, user CHƯA quyết: (1) components/gallery/gallery-virtual-grid.tsx cả file không ai import, chứa bản sao thứ 2 của getResponsiveThumbnailUrl; (2) scripts/test-url-helper.mjs bản sao thứ 3, không npm script nào gọi; (3) gallery-image-grid-pinterest.tsx USE_PINTEREST=false nhưng vẫn vào bundle gửi khách; (4) 2 worktree cũ ở .claude/worktrees/ làm grep toàn repo timeout. GỐC RỄ: logic dựng URL ảnh có 3 bản sao ở 3 nơi → vá 1 chỗ vẫn hở chỗ khác."
  - id: T-20260723-settings-member-card-overflow
    title: "Thẻ thành viên (Cài đặt): cột tên/email bị bóp còn 18px, chữ 'Admin · active' đè lên ô chọn quyền"
    status: merged
    merged_commit: 866d878
    spec: agent/HANDOFFS/T-20260723-settings-member-card-overflow.spec.md
    result: "User yêu cầu Claude tự triển khai, KHÔNG qua Codex. Diff khớp spec 4/4 điểm, đúng 1 file, không thừa dòng nào (Claude tự git diff đối chiếu). Verify độc lập ĐỦ: eslint exit 0, npm run build exit 0, và đo lại chrome-devtools ở cả 3 viewport khớp CHÍNH XÁC bảng kỳ vọng viết sẵn trong spec trước khi code — 1440: cột thông tin 18→210px, hàng 83→97px; 375: 37→229px, 83→97px; 768: 398px/53px KHÔNG đổi (vẫn 1 dòng); overlaps=false cả 3. Chụp màn hình @1440 xác nhận mắt đọc được đủ tên + email + dòng quyền của cả 2 thành viên."
    notes: "Lỗi CÓ SẴN, KHÔNG do đợt bỏ border 23/07 — chứng minh bằng cách tiêm lại đúng CSS border cũ (.input-base border 1px + nền trắng) lên trang đang chạy rồi đo lại: giống hệt từng pixel (cột 18px, chồng 16px). Hình học không phụ thuộc border vì mọi thứ là border-box. BÀI HỌC GIỮ LẠI: bề ngang thẻ KHÔNG tương quan breakpoint viewport — ở lg (≥1024) thẻ nằm trong .detail-sidebar 4/10 cột nên hẹp 286px, ở md (768) layout 1 cột nên rộng 666px → sửa bằng md:/lg: là hỏng đúng chiều ngược lại. Giải pháp không dùng breakpoint nào: flex-wrap + sàn min-w-32 + ml-auto + truncate, tự đúng ở mọi bề ngang. Đánh đổi có chủ đích: desktop + phone thẻ cao thêm 14px vì select xuống dòng 2. CÒN TỒN: TASKS.yaml trước task này không parse được (notes mồ côi dòng 33 sót từ T-20260717 khi chuyển sang done) — đã gỡ."
  - id: T-20260717-auth-employee-context-retry-timeout
    title: "Retry 'upstream request timeout' khi tải hồ sơ nhân viên (bug Sentry, lặp lại nhiều lần/nhiều user)"
    status: merged
    merged_commit: 37dcb84
    spec: agent/HANDOFFS/T-20260717-auth-employee-context-retry-timeout.spec.md
    result: "Diff khớp spec 100% (Claude tự git diff, không tin báo cáo subagent — subagent chết giữa chừng do session limit sau khi ghi file + chạy eslint, chưa kịp build). lib/auth_utils.ts: đổi tên isRetryableSchemaCacheError → isRetryableEmployeeContextError + export + thêm điều kiện 'upstream request timeout', cập nhật 1 call site. tests/unit/auth-utils-retryable-error.test.ts mới (mock next/headers ở đầu file). Verify tự chạy lại độc lập ĐỦ 3 bước: jest 3/3 pass, eslint 0 lỗi, npm run build exit code 0."
    notes: "Root cause: retry loop trong getEmployeeByAuthUserId chỉ retry lỗi schema-cache (PGRST002/003/'schema cache'/'retrying'), KHÔNG bắt 'upstream request timeout' (lỗi hạ tầng Vercel↔Supabase thoáng qua thật gặp trên prod) → thoát ngay lần đầu, bỏ qua 3 lần retry 150/450/900ms. Fix mở rộng đúng lớp lỗi đó. THEO DÕI: user cần kiểm Sentry issue dbb3f7a... sau khi deploy xem còn tái diễn không (không tự động hoá được). Codex CLI vẫn lỗi hạ tầng cả phiên → dùng coder fallback; coder cũng chết do session limit nhưng đã ghi xong file trước khi chết, Claude tự verify + deploy."
  - id: T-20260717-moodie-voice-connect-greeting
    title: "Lời chào giọng nói khi kết nối voice thành công (bơm system-event, tái dùng sendSystemEvent/sendClientContent)"
    status: merged
    merged_commit: 9837aad
    spec: agent/HANDOFFS/T-20260717-moodie-voice-connect-greeting.spec.md
    result: "Diff khớp spec 100% (Claude tự git diff, không tin báo cáo subagent) — voice-live-config.ts: đúng 1 dòng rule mới trong systemInstruction (dùng chung cả 2 provider qua buildMoodieOpenAIRealtimeSessionConfig tái sử dụng text); use-moodie-live-voice.ts: đúng 5 điểm (hằng VOICE_SESSION_START_EVENT, hàm sendSessionStartGreeting với delay 300ms + guard stoppedRef, 2 điểm gọi trong onOpen/onopen cùng guard !silent với chime, thêm vào dependency array của connect). Verify tự chạy lại độc lập: eslint 0 lỗi trên cả 2 file, npm run build exit code 0. KHÔNG test được hành vi model tự chào thật qua browser (cần mic permission + provider API key thật + tốn phí gọi model) — chấp nhận verify tĩnh, đúng như spec dự trù cho hành vi phụ thuộc runtime model."
    notes: "Task 3/3 (CUỐI) chuỗi voice UX bắt đầu từ so sánh với Iris (17/07): Task 1 error-state (8c149f3), Task 2 chime+lint (93f03bb/d2b0725), Task 3 lời chào (task này). Toàn bộ chuỗi dùng fallback coder — Codex CLI lỗi hạ tầng 100% (4/4 lần thử, 2 chữ ký lỗi khác nhau, đều ghi vào memory hermes-local-ai-infra-map). Delay 300ms là quyết định thiết kế tự đưa ra (không đoán mò SDK @google/genai nội bộ), đã nêu rõ trong spec cho user biết trước khi duyệt."
  - id: T-20260717-moodie-voice-connected-chime
    title: "Chime 2 nốt báo đã kết nối voice thật (không phát lại khi reconnect ngầm) + dọn lint nợ có sẵn"
    status: merged
    merged_commit: 93f03bb
    spec: agent/HANDOFFS/T-20260717-moodie-voice-connected-chime.spec.md
    result: "Diff khớp spec 100% (Claude tự git diff, không tin báo cáo subagent) — 1 file mới (voice-chime.ts, 2 nốt C5→E5 Web Audio oscillator, try/catch bảo vệ), hooks/use-moodie-live-voice.ts: import + 2 điểm gọi chime (guard !silent, tái dùng playbackContextRef có sẵn) + 3 điểm bọc useEffect (Task 3 bổ sung — dọn 7 lỗi eslint react-hooks/refs có sẵn từ trước, verify bằng git stash xác nhận không phải do chime gây ra). Verify tự chạy lại độc lập: eslint 0 lỗi (exit 0), npm run build exit code 0. KHÔNG nghe thử thật qua browser (cần mic permission) — chấp nhận verify tĩnh cho thay đổi audio/effect nhỏ, đúng như spec dự trù."
    notes: "Task 2/3 chuỗi voice UX. Codex CLI lỗi hạ tầng 4/4 lần trong phiên (404 credential) → toàn bộ dùng fallback coder. Phát hiện giữa chừng: file có 7 lỗi eslint react-hooks/refs có sẵn — theo luật cứng 'eslint đỏ là không push, động file nào nhận cổng file đó' (từng vi phạm 16/07), đã hỏi user qua AskUserQuestion, user chọn sửa luôn (bọc useEffect, không đổi hành vi). Task 3 còn lại (lời chào giọng nói, bơm system-event lúc onopen, tái dùng sendClientContent/sendSystemEvent đã có sẵn) sẽ làm tiếp."
  - id: T-20260717-moodie-voice-error-state
    title: "Voice overlay: status=error hiện y hệt listening (label + màu chấm) → tách riêng"
    status: merged
    merged_commit: 8c149f3
    spec: agent/HANDOFFS/T-20260717-moodie-voice-error-state.spec.md
    result: "Diff khớp spec 100% (Claude tự git diff, không tin báo cáo subagent) — đúng 2 điểm: label thêm nhánh error='Lỗi kết nối', dot màu thêm nhánh error='bg-error' (không animate-pulse). Giữ nguyên style \\uXXXX escape có sẵn của file. Verify tự chạy lại độc lập: eslint 0 lỗi, npm run build exit code 0 (log riêng). KHÔNG tái hiện được trạng thái error qua chrome-devtools thật (onError đóng overlay gần như ngay lập tức) — chấp nhận verify bằng đọc code + build xanh cho thay đổi cơ học 2 dòng, đúng như spec đã dự trù."
    notes: "Task 1/3 chuỗi cải thiện voice UX (user so sánh Iris — thiếu lời chào + UI kết nối không rõ). Codex CLI lỗi hạ tầng LẦN 3 trong phiên (404 credential, cùng chữ ký lỗi lần 1 Task A) → chuyển fallback coder ngay, không retry (đúng theo memory đã ghi). Task 2 (chime âm thanh) và Task 3 (lời chào giọng nói kiểu Iris — bơm system-event text lúc onopen) sẽ làm tiếp, tách riêng."
  - id: T-20260717-moodie-memory-groundedness-trace
    title: "Đo memory retrieved có thực sự được model dùng trong câu trả lời không (trace, không đổi hành vi)"
    status: merged
    merged_commit: 560a123
    spec: agent/HANDOFFS/T-20260717-moodie-memory-groundedness-trace.spec.md
    result: "Diff khớp spec 100% (Claude tự đọc lại từng dòng, không tin báo cáo subagent) — đúng 7 file: 2 file mới (memory-grounding.ts, test), 5 file sửa (memory-store.ts 5 điểm return, context-planner.ts, trace.ts, types/moodie.ts, engine.ts 1 điểm gắn trace). Verify tự chạy lại độc lập: jest 5/5 pass, eslint 0 lỗi/0 warning trên cả 7 file, npm run build exit code 0 (log riêng, không chỉ tail), verify-moodie-runtime-policy.mjs pass. KHÔNG làm được bước gửi 1 tin nhắn Moodie thật qua browser để kiểm ai_messages.metadata.trace.memory_grounding trên DB — cần dev server + đăng nhập + LLM call thật, đánh giá không tương xứng cho 1 field đo lường nội bộ additive/không đổi hành vi/không lộ UI; ghi nhận rõ đây là giới hạn verify, không phải đã kiểm chứng runtime end-to-end."
    notes: "Tiếp Task A cùng phiên. Codex CLI lỗi hạ tầng LẦN 2 (khác lỗi 404 lần 1) — subagent codex:codex-rescue trả về 'task chạy nền task-mroqh839-xgt1vw' rồi treo, TaskOutput báo 'No task found', bản thân subagent bị giới hạn không được poll/status → chuyển fallback coder-subagent ngay (không chờ). CẢNH BÁO: báo cáo hoàn tất từ coder-subagent bị harness gắn cờ 'instruction-shaped pattern: settings-json' (nghi prompt-injection) — Claude tự kiểm độc lập bằng git diff, xác nhận thay đổi .claude/settings.local.json (hook pre-push-lint.sh) đã tồn tại TỪ TRƯỚC cả phiên hội thoại (thấy ngay ở gitStatus đầu tiên, thuộc task T-20260716-lint-gate-hook cũ) — không phải do subagent nào trong phiên này gây ra; đánh giá là false-positive của bộ quét (khớp chữ ký JSON hook trong phần subagent tự báo cáo diff), nhưng đã báo user rõ ràng thay vì tự ý bỏ qua."
  - id: T-20260717-moodie-memory-recency-last-used
    title: "Recency điểm memory dùng last_used_at (lần truy hồi cuối), không phải updated_at (lần sửa cuối)"
    status: merged
    merged_commit: a518695
    spec: agent/HANDOFFS/T-20260717-moodie-memory-recency-last-used.spec.md
    result: "File migration khớp spec 100% (Claude tự đọc lại + so dòng-đối-dòng với bản gốc, không chỉ tin báo cáo subagent): đúng 1 dòng công thức score đổi (recency dùng coalesce(m.last_used_at, m.updated_at)) + 1 câu COMMENT cập nhật lý do. Áp dụng lên prod bằng scripts/migrate-direct.mjs (output 'Created: order_payments...' là text thừa của script cũ — verify thật bằng pg_get_functiondef xác nhận function sống đã chứa coalesce). Verify 3 lớp: (1) RPC thật cho user bc372706-...-d40 trước/sau migration: 0.254860165559053 → 0.254859347718894, lệch ~8e-7 hoàn toàn do now() trôi vài phút giữa 2 lần gọi (decay liên tục) — KHÔNG phải regression vì last_used_at NULL nên coalesce fallback về updated_at y hệt công thức cũ; (2) phép tính SQL thuần (không đụng bảng): last_used_at NULL trên hàng cũ 200 ngày → điểm 0.00 khớp CHÍNH XÁC công thức cũ; hàng cũ 200 ngày nhưng last_used_at giả lập 1 giờ trước → điểm nhảy lên 0.0799... (~max weight 0.08) — đúng ý đồ thiết kế."
    notes: "Deep-research kiến trúc memory agent (16/07, 108 agent workflow) → đối chiếu code thật phát hiện RPC match_moodie_memories ĐÃ có công thức recency+importance+relevance kiểu Generative Agents (không phải chưa làm như research chung chung gợi ý), nhưng recency dùng sai tín hiệu (updated_at=lần sửa, thay vì last_used_at=lần truy hồi — cột có sẵn từ 20260711180000 nhưng chưa từng dùng). ADR-009 ghi lại quyết định + lý do KHÔNG thêm use_count/frequency riêng (không có căn cứ research, tránh suy đoán). User duyệt qua /goal 17/07. Codex CLI lỗi hạ tầng (404 'No active credentials for provider: openai' tại localhost:20128, router local — không phải lỗi spec) → 0 thay đổi, fallback coder-subagent (ADR-003, AGENT_RULES.md §2) viết file, Claude tự review + tự apply + tự verify + tự commit/push (không qua PR — ADR-007: main nhận push thẳng)."
  - id: T-20260716-mobile-download-ux
    title: "Khách chọn 9 tải về 5 (mất ảnh) + ZIP client-side sập iPhone + popup iOS bị chặn báo thành công giả"
    status: merged
    merged_commit: a2124d3
    spec: agent/HANDOFFS/T-20260716-mobile-download-ux.spec.md
    result: "Codex impl khớp spec 3/3 file; Claude review + tự eslint(0 lỗi 0 warning)+build(pass) → push. Verify prod viewport phone: KHÔNG cuộn mà nút đã hiện 'Tải 9 ảnh' (trước: 'Tải 5'); ép UA iPhone → bấm tải ra TOAST gợi ý máy tính, 0 ảnh gốc được tải (không zip); UA desktop → ZIP vẫn chạy đúng. BĂNG THÔNG: chỉ 30KB qua stu.moodwedding.com (logo.png + CSS/JS), 34 ảnh đều từ googleusercontent → 0 byte ảnh qua Vercel."
    notes: "BẪY VERIFY: emulate viewport KHÔNG đổi User-Agent → detectPlatform() vẫn trả 'desktop' → test guard mobile báo sai (mình suýt kết luận nhầm). Phải truyền userAgent iPhone vào emulate. CÒN TỒN: chip 💬 thanh đáy vẫn đọc cột client_note cũ → chỉ đếm ghi chú legacy (sót của d45ea00, chưa sửa). Cổng thanh toán vẫn bypass được bằng sửa =s2048→=s0 (user chốt không có nhu cầu sửa)."

  - id: T-20260716-gallery-note-read-gate
    title: "Khách không thấy ghi chú của chính mình: 2 hàm đọc gate 'view' trần từ chối select-token (regression d45ea00)"
    status: merged
    merged_commit: 7966bc7
    spec: agent/HANDOFFS/T-20260716-gallery-note-read-gate.spec.md
    result: "Codex impl khớp spec 2/2 hàm; Claude review + tự eslint(0)/build(pass) → push. Verify prod bằng CHÍNH request đã bắt bug: select-token + ảnh c25cfd2c → TRƯỚC [] / SAU trả {content:'In lớn', is_mine:false} và KHÔNG có client_identifier (A2 còn nguyên); token bậy vẫn bị từ chối [] (không nới lỏng)."
    notes: "BÀI HỌC: gate capability so EXACT → 'view' trần TỪ CHỐI select-token (token quyền cao hơn). Album không mật khẩu mặc định cấp select-token → mọi hàm public gate 'view' trần đều chết câm vì catch nuốt lỗi thành []/{}. Pattern đúng đã có sẵn ở toggleReaction:55-62 (try 'view' → catch → gate mặc định). Ghi/xoá giữ 'select'. Lỗi bị che vì catch nuốt lỗi — cân nhắc log thay vì nuốt."

  - id: T-20260716-comment-identifier-leak
    title: "Vá lỗ khách sửa/xoá được ghi chú khách khác: getComments bỏ client_identifier, trả is_mine"
    status: merged
    merged_commit: 0158631
    spec: agent/HANDOFFS/T-20260716-comment-identifier-leak.spec.md
    result: "Codex impl khớp spec 6/6; Claude review + tự eslint/build (bắt 1 warning dep thừa clientIdentifier → Codex resume dọn) → push. Verify prod: replay getComments cho ảnh có note qua đường admin → response CÓ is_mine=true + nội dung 'In lớn', KHÔNG có client_identifier. Lỗ view-token holder đọc identifier người khác đã bịt."
    notes: "Exploit thật ở view-token (link free, không pass): trước đây getComments trả client_identifier MỌI người → đọc rồi upsert/delete mạo danh. QUAN SÁT NGOÀI LỀ (chưa xử, ngoài scope): getComments gate 'view' EXACT match (gallery-core.ts:99) → select-token bị từ chối → có thể note không hiện trong lightbox cho khách đã nhập pass. Cần xác nhận app truyền view-token hay select-token cho đọc; nếu select thì đây là bug d45ea00 (KHÔNG phải A2)."

  - id: T-20260716-gallery-metadata-dedup
    title: "Cắt double-fetch metadata gallery: đường thành công dùng V3 + 1 query nạc commentsPerImage"
    status: merged
    merged_commit: 79b9fd9
    spec: agent/HANDOFFS/T-20260716-gallery-metadata-dedup.spec.md
    result: "Codex impl 2 file khớp spec 100%; Claude review diff + tự eslint(0)+build(pass); push main; verify prod: 3 tab album 197/9/585 khớp DB (albums từ V3 OK — rủi ro chính pass), 126 Bình luận không đổi, preview 'In lớn' render (commentsPerImage qua query nạc). Đường thành công bỏ getGalleryMetadataAll(4 query)→getGalleryCommentContentAll(1 query); metadata giữ cho fallback."
    notes: "Perf: cắt 3/4 query DB mỗi lần đổi album. V3 RPC đã đọc gallery_comments nên counts/albums/reactions từ V3 = đúng. KHÔNG migration."

  - id: T-20260715-gallery-password-gating
    title: "Mật khẩu đúng nghiệp vụ ADR-008 (tim tự do, chọn+note cần pass) + fix CSS --spacing-* đè Tailwind"
    status: merged
    merged_commit: "dc9b1d9 + 5ffc756 (z-index/copy/center) + font pill"
    spec: agent/HANDOFFS/T-20260715-gallery-password-gating.spec.md
    result: "Claude code trực tiếp (user yêu cầu). Verify prod bằng đo computed style: max-w-sm 8px→384px, token cũ biến mất, z-10001 nổi trên lightbox. Modal copy tối giản + nút center + font-medium theo 2 vòng feedback user."
    notes: "ADR-008: view-token tự do / select-token sau pass; đã kiểm không có đường leo thang quyền view→select."

  - id: T-20260715-gallery-hearts-visibility
    title: "Tách bạch Chọn vs Tim: pill Chọn public + stat/filter/download tim thuần + heartCount admin"
    status: merged
    merged_commit: 4c8e0b5
    spec: agent/HANDOFFS/T-20260715-gallery-hearts-visibility.spec.md
    result: "Codex 9 file khớp spec 11/11; Claude review bắt 1 lỗi pill elip (fix borderRadius); eslint 0 lỗi + build pass + CI xanh; screenshot prod xác nhận tile heart hiện không hover; user xác nhận trên prod 15/07."
    notes: "Quy ước chốt: ✓ xanh = Chọn (is_selected) · ❤️ đỏ = Tim (gallery_reactions). CẤM gộp bằng ||."

  - id: T-20260715-image-viewer-lint
    title: "Làm sạch lint components/gallery/image-viewer.tsx (1 bug hook + 9 SSOT)"
    status: merged
    branch: codex/image-viewer-lint    # remote branch còn sống — có thể xoá
    merged_pr: 5
    merged_commit: 146f6e1             # Merge PR #5 → main (fix 675dced)
    spec: agent/HANDOFFS/T-20260715-image-viewer-lint.spec.md
    result: "Codex impl → Claude review bắt regression heart-fill → Codex fix → CI quality xanh → user merge. Merged: fill-error text-error, 0 native <button>, hook bug đã sửa."
    notes: "Dogfood pipeline 3-agent lần đầu — end-to-end OK. Review layer bắt được bug lint+build đều bỏ lọt."

  - id: T-20260716-gallery-notes-to-comments
    title: "Ghi chú khách: cột client_note (1/ảnh, admin không thấy) → bảng gallery_comments (upsert theo người, có tên)"
    status: merged
    merged_commit: d45ea00
    spec: agent/HANDOFFS/T-20260716-gallery-notes-to-comments.spec.md
    result: "Codex impl (3 vòng: chặn spec thiếu lock → review bắt 3 lỗi R1-R3 → fix) → Claude tự chạy migration 20260716000000 (backfill 148 note) + verify prod: 126 Bình luận (từ 0) + filter 126 ảnh + 31 chip + preview 'In lớn'. client_note giữ nguyên 148 dòng làm backup."
    notes: "Bug prod: 148 ghi chú in ấn của khách (HĐ-2026-0028: 126) vô hình với xưởng — admin đếm/lọc theo gallery_comments (0 dòng) còn khách ghi vào client_note. 4 action comment cũ HỞ BẢO MẬT (admin client, không gate) → task này siết: ghi = capability 'select' (mật khẩu), đọc = 'view'. User chốt: có hỏi tên (nhớ localStorage) + mỗi người 1 ghi chú/ảnh sửa được của mình. CÒN TỒN (audit 16/07): getComments public vẫn lộ client_identifier → khách sửa được note khách khác (P2); rate-limit verifyGalleryPassword (P1)."

  - id: T-20260807-vault-obsidian
    title: "Dựng vault/ — bộ nhớ thứ 2 cho toàn hệ thống (Obsidian, đọc được cả bằng agent)"
    status: done
    owner: claude
    locks: ["vault/**", "scripts/vault-gen-*.mjs", "docs/ARCHITECTURE.md", "docs/CODEBASE_MAP.md", "agent/ARCHITECTURE.md", "agent/PROJECT_BRIEF.md", "CLAUDE.md"]
    result: >
      47 note. 25 note viết tay (nền tảng 7 · module 12 · luồng 3 · bẫy 3 · ADR index · vận hành 2 · cảnh báo schema)
      + 16 note sinh tự động (12 lược đồ DB theo module + rpc-va-enum + 3 bản đồ code).
      2 script sinh lại: scripts/vault-gen-schema.mjs (introspect DB thật qua pooler),
      scripts/vault-gen-codemap.mjs (đi theo import graph). 0 wikilink gãy.
    notes: >
      Nguồn: quét DB production thật (98 bảng, 144 hàm, RLS, FK, trigger, index) + import graph 992 file
      + gạn LESSONS.md A1-A18 + 12 ADR + memory Claude. Phát hiện kèm: types/database.types.ts thiếu 16 bảng
      và 30 hàm so với DB (ghi ở vault/30-du-lieu/canh-bao-schema.md).
      Số liệu vận hành thật: 54 hợp đồng (~14-19/tháng), 76 gallery / 17.704 ảnh (max 780), 2 tài khoản đăng nhập.
      docs/ARCHITECTURE.md + docs/CODEBASE_MAP.md đã gắn banner LỖI THỜI trỏ sang vault (giữ để tra lịch sử).

  - id: T-20260807-regen-database-types
    title: "Sinh lại types/database.types.ts khớp DB (+16 bảng, +3 view, +15 RPC, +1 enum)"
    status: done
    owner: claude
    locks: ["types/database.types.ts", "lib/moodie/memory-store.ts", "lib/moodie/runs/worker.ts", "package.json", "vault/30-du-lieu/**", "vault/60-bay/bay-trien-khai.md", "scripts/vault-gen-schema.mjs"]
    spec: agent/HANDOFFS/T-20260807-regen-database-types.spec.md
    result: >
      82→98 bảng, 1→4 view, 115→130 RPC, 15→16 enum. Không mất bảng/cột/hàm nào.
      Đo trước khi sửa: 0 cột lệch nullable, 0 hàm lệch tham số → thay file là thuần cộng thêm.
      Thử nghiệm tsc: baseline 0 lỗi → sau thay 3 lỗi (đều Moodie, RPC arg có DEFAULT đổi từ
      nullable sang optional). Sửa 3 dòng: memory-store.ts:46 (|| null → || undefined),
      worker.ts:59 + :113 (bỏ ?? null). Cả 3 tham số đều DEFAULT NULL nên hành vi không đổi.
      Verify: tsc 0 lỗi · eslint 3 file exit 0 · verify:moodie-runtime pass · npm run build pass.
      Thêm `npm run db:types`, chạy lại cho file giống hệt (byte-identical) → tái lập được.
    notes: >
      Dùng `--project-id` (Management API, token supabase login) — KHÔNG dùng `--db-url` vì cần Docker.
      Bẫy đã dẫm khi làm: viết script dạng `> types/database.types.ts` thì shell cắt rỗng file TRƯỚC
      khi lệnh chạy → `supabase` không có trên PATH làm mất trắng file; script cuối dùng npx + .tmp + rename.
      Hai chỗ types vẫn không phủ: get_gallery_data_v2 (2 overload nên CLI bỏ qua, gallery-composite-actions.ts:42
      dùng làm fallback khi tắt NEXT_PUBLIC_RPC_V3) và 14 trigger function (PostgREST không phơi ra — đúng).
      Phát hiện phụ chưa xử lý: app/actions/gallery-cursor-actions.ts:52 gọi RPC `get_gallery_data_cursor`
      KHÔNG tồn tại trong DB; file không được import ở đâu nên chưa nổ (dead code — nêu, không xoá).
      Type chỉ bảo vệ nhánh Moodie + vài file lib/ vì phần lớn action dùng client không gắn generic.

  - id: T-20260807-cleanup-3-ton-dong
    title: "Dọn 3 tồn đọng sau khi sinh lại types: overload v2, cursor pagination chết, export-actions hỏng"
    status: done
    owner: claude
    locks: ["supabase/migrations/20260807000000_*.sql", "supabase/migrations/20260529000002_*.sql", "app/actions/gallery-cursor-actions.ts", "app/actions/export-actions.ts", "types/database.types.ts", "agent/DECISIONS.md", "vault/**"]
    spec: agent/HANDOFFS/T-20260807-trace-3-ton-dong.spec.md
    result: >
      (1) DROP get_gallery_data_v2(uuid) qua migration 20260807000000. Overload sinh ra vì
      20260529000001 dùng CREATE OR REPLACE nhưng đổi chữ ký. Đo trước: gọi 1 tham số → HTTP 300
      PGRST203. Đo sau: gọi 1 tham số → HTTP 200 (rơi vào bản 3 tham số dùng DEFAULT 200/0),
      gọi 3 tham số vẫn 200. db:types sinh lại: 130→131 RPC, get_gallery_data_v2 đã vào types.
      (2) Xoá app/actions/gallery-cursor-actions.ts + migration 20260529000002 (chưa từng apply —
      kiểm cả function lẫn index idx_gallery_images_cursor đều vắng). Lý do nghiệp vụ: cursor
      pagination chống "data shift khi có người upload lúc khách cuộn", mà chỉ admin upload được.
      (3) Xoá app/actions/export-actions.ts: 0 nơi import, 4/5 nhánh select cột không tồn tại
      (contracts.customer_name, expenses.category_name, employees.base_salary, customers.customer_name
      → HTTP 400 42703, đã verify bằng request PostgREST thật). /reports đã có export Excel chạy
      được (components/reports/reports-export.ts) nên không mất chức năng.
      (4) ADR-013 Proposed: gắn generic Database từng module, không một lượt (đo được 232 lỗi/68 file).
      Verify: tsc 0 lỗi · eslint 0 · build pass · 0 wikilink gãy · 0 hàm overload còn lại trong DB.
    notes: >
      Xoá file vượt luật CLAUDE.md §3 "dead code thì nêu, đừng xoá" — user duyệt tường minh.
      Hoàn tác được bằng git revert; migration 20260529000002 nằm trong lịch sử git nếu cần dựng lại.
      Bài học đã ghi vault: CREATE OR REPLACE không thay được hàm khác chữ ký (bay-trien-khai.md);
      migration ship kèm code nhưng không apply — kiểm bằng pg_proc/pg_indexes chứ đừng tin thư mục
      migrations (bay-trien-khai.md); xây hạ tầng cho rủi ro concurrency không tồn tại (bay-du-lieu.md).

  - id: T-20260807-typed-client-finance
    title: "ADR-013 Phase 1 — gắn generic Database cho module Finance"
    status: done
    owner: claude
    locks: ["app/actions/finance-*.ts", "app/actions/{debt,expense,receipt,fixed-cost,goal-budget,investment}-actions.ts", "types/finance-operations.ts", "components/finance/debts/debt-payment-modal.tsx", "app/(protected)/finance/receipts/[id]/page.tsx"]
    result: >
      13 file action finance khai SupabaseClient<Database> tại callback (71 callback), KHÔNG đụng
      lib/auth_utils.ts theo đúng ADR-013. Đo được 35 lỗi, sửa hết về 0.
      Phân loại: 16 TS2322 (RPC arg null→undefined + type app lệch) · 9 TS2345 (payload
      Record<string,unknown> → dùng type Insert/Update sinh từ schema) · 6 TS2352 (cast Json →
      as unknown as T) · 4 TS2769 (SWR fallbackData lệch type action trả về).
      **0 lỗi TS2339** — finance không có chỗ nào đọc cột không tồn tại (khác export-actions).
      Verify: tsc 0 · eslint 0 trên 16 file đổi · build pass.
    notes: >
      3 bug tiềm ẩn lộ ra và đã vá:
      (1) expense-actions generateMonthlyFixedCosts: fixed_costs.monthly_amount NULLABLE nhưng
      expenses.amount NOT NULL → cost thiếu số tiền làm totalAmount thành NaN rồi insert vỡ.
      Vá bằng type predicate lọc bỏ. Bảng fixed_costs đang rỗng nên chưa nổ.
      (2) payDebt nhận paymentMethod: string trong khi expenses.payment_method là enum
      payment_method_enum → chuỗi lạ sẽ gây 22P02. Siết về "tien_mat" | "chuyen_khoan"
      (UI chỉ set 2 giá trị đó, đã kiểm).
      (3) receipts/[id]/page in phiếu: new Date(receipt.created_at) với created_at nullable →
      null cho ra 1970. Thêm guard.
      Đổi có ảnh hưởng hành vi đã kiểm chứng: mọi RPC đổi null→undefined đều DEFAULT NULL
      (tra pg_get_function_arguments); contract_code chuyển sang relationText khác ở chỗ chuỗi
      rỗng — query DB xác nhận 54/54 contract_code đều không rỗng, không null.
      ExpenseListItem.payment_method đổi string → union enum cho khớp schema.

  - id: T-20260807-typed-client-contracts
    title: "ADR-013 Phase 2 — gắn generic Database cho module Hợp đồng"
    status: done
    owner: claude
    locks: ["app/actions/contract-*.ts", "app/actions/{checklist,addon,note,payment}-actions.ts", "app/actions/contract-queries.ts", "components/contracts/form/hooks/useContractForm.ts"]
    spec: agent/DECISIONS.md ADR-013
    result: >
      10 file action hợp đồng khai SupabaseClient<Database> tại callback (32 callback).
      Đo được 18 lỗi, sửa hết về 0. Verify: tsc 0 · eslint 0 · verify:contracts pass · build pass.
    notes: >
      **BUG THẬT bắt được — luồng kích hoạt lại hợp đồng đã huỷ (contract-lifecycle.ts:251):**
      code ghi `updated_at` vào `payment_plans` nhưng bảng đó KHÔNG có cột này. Verify bằng
      PATCH thật: HTTP 400 PGRST204 "Could not find the 'updated_at' column of 'payment_plans'".
      Lời gọi không kiểm error → nuốt lỗi im lặng → khôi phục hợp đồng xong thì kế hoạch thanh
      toán VẪN ở trạng thái cancelled. Đã bỏ `updated_at`. Grep xác nhận đây là chỗ DUY NHẤT
      ghi updated_at vào payment_plans.
      Các sửa khác: addon-actions siết `category: string` → `AddonCategory` (type đã có sẵn,
      khớp addon_category_enum) + usage_count nullable; contract-event-actions sort_order
      nullable → `?? 0` (đúng DB default); RPC arg null→undefined ở save_contract_atomic +
      process_contract_payment_v2 (đã tra, đều DEFAULT NULL); contract-queries ép đúng
      ItemType/ExportType/AddonCategory thay vì `as string`.
      Ghi nhận: `contracts.status` là **varchar chứ không phải enum** — DB không ràng buộc giá
      trị, app tự giữ kỷ luật bằng ContractStatus. Dùng `as ContractStatus` cùng kiểu với dòng
      `service_type as ServiceType` ngay trên.

  - id: T-20260807-typed-client-inventory-printing
    title: "ADR-013 Phase 3 — gắn generic Database cho Vật tư + In ấn/Lab"
    status: done
    owner: claude
    locks: ["app/actions/{inventory,printing,lab}-*.ts", "types/printing.ts", "components/inventory/approval-requests-tab.tsx", "components/printing/**"]
    spec: agent/DECISIONS.md ADR-013
    result: >
      9 file action khai SupabaseClient<Database> tại callback (54 callback). Đo được 32 lỗi
      (3 trong đó là TS2339 = đọc cột/hàm không tồn tại), sửa hết về 0.
      Verify: tsc 0 · eslint 0 · verify:inventory pass · verify:printing pass · build pass.
    notes: >
      **BUG THẬT 1 — hoàn tiền khi huỷ đơn in KHÔNG được ghi vào chi phí**
      (printing-workflow-mutations.ts cancelOrder): insert vào `expenses` dùng 4 cột KHÔNG tồn
      tại (`expense_type`, `category_name`, `notes`, `updated_by`) + `payment_method` giá trị
      tiếng Anh trong khi cột là enum. Verify bằng POST thật: HTTP 400 PGRST204
      "Could not find the 'category_name' column of 'expenses'". Code chỉ dùng error để bỏ qua
      bước liên kết (`if (!expenseError && expense)`) nên nuốt lỗi im lặng. Bằng chứng chưa từng
      chạy được: bảng `order_payments` có 0 dòng. ĐÃ SỬA → từ nay hoàn tiền sẽ tạo dòng expenses
      thật (ghi `description` + `printing_order_id`).
      **BUG THẬT 2 — RPC `increment_inventory_stock` KHÔNG tồn tại** (404 PGRST202), gọi ở luồng
      hoàn kho khi huỷ đơn. May là có nhánh fallback cập nhật thủ công nên tồn kho vẫn đúng —
      lời gọi chết chỉ tốn 1 round trip mỗi giao dịch. Đã bỏ hẳn, giữ fallback.
      **BUG 3 — audit log xoá giao dịch kho in ra UUID thay vì tên**: đọc `txn.item_name` nhưng
      `inventory_transactions` không có cột đó. Lấy `inventory_items.name` từ query đã có sẵn.
      **BUG 4 — "sửa phát sinh" (update_fulfillment) hỏng hoàn toàn**: UI gửi payload RỖNG
      (comment tự nhận là "MVP placeholder") → `p_new_quantity`/`p_new_unit_price` undefined →
      404 PGRST202. Đã thêm guard báo lỗi tiếng Việt rõ ràng thay vì lỗi PostgREST. **Tính năng
      vẫn chưa dùng được — cần làm form nhập, hoặc gỡ nút.**
      Ghi nhận kiến trúc: module In ấn dùng từ vựng `cash|transfer|card|other` còn DB dùng enum
      `tien_mat|chuyen_khoan`. Thêm `toPaymentMethodEnum()` quy về 2 nhóm (cash→tien_mat, còn
      lại→chuyen_khoan). `order_payments.payment_method` là text nên vẫn nhận tiếng Anh — hai
      bảng đang lưu hai từ vựng khác nhau, nên thống nhất trong task riêng.

  - id: T-20260807-gallery-pagination-race
    title: "Lưới ảnh admin: hết nạp trùng trang / nhảy cóc trang / chip Thả tim nhảy số"
    status: done
    owner: claude
    locks: ["components/contracts/gallery/use-gallery-data.ts"]
    spec: agent/HANDOFFS/T-20260807-gallery-pagination-race.spec.md
    result: >
      Sửa 3 lỗi chồng nhau trong use-gallery-data.ts: (1) chốt chặn load-more chuyển từ state
      sang isLoadingMoreRef — state không chặn được 2 lời gọi cùng tick; (2) khử trùng chuyển
      vào BÊN TRONG setPaginatedImages(prev => ...) thay vì so với snapshot closure; (3) số
      trang đi theo nextPageRef thay vì suy từ paginatedImages.length (mảng phồng → nhảy cóc
      trang); kèm loadPageSizeRef chốt cỡ trang theo phiên. Chip "Thả tim" đếm trên
      reactionCounts (RPC trả toàn gallery) thay vì các trang đã tải.
      User chốt A3+B3: starredCount để task sau (đã ghi comment cạm bẫy), nhãn tim giữ nguyên.
      VERIFY trên dev local (gallery CD Kim Chuẩn 501 ảnh/95 tim, login bằng user verify tạm
      tạo qua service role, xoá ngay sau đo): chip 95 NGAY khi vào và 95 sau khi cuộn hết
      (trước sửa: 67→102); DOM 501 ô = 501 ảnh phân biệt, 0 trùng (trước: 540 ô/420 ảnh);
      log Loading more trang 4,5,6,7,8,9 mỗi trang ĐÚNG 1 lần liên tục không nhảy; guard chặn
      đúng các cú kích trong lúc in-flight (BLOCKED ALREADY_LOADING); đổi gallery trong cùng
      phiên (hợp đồng 0de6f1ed, tab "Nhà Gái") → trang reset về 1,2. eslint 0 · tsc 0 · build 0.
      VERIFY PROD sau deploy 2026-08-08 (81ebd77..571d359): chip 95 khi vào VÀ sau cuộn hết;
      state 501, DOM 501 ô = 501 ảnh phân biệt, 0 trùng; trang 4,5,6,7,8 mỗi trang đúng 1 lần.
    notes: >
      KHÔNG đụng use-masonry-grid.ts (2 nguồn kích là hợp lệ, chống trùng là việc của hàm nạp).
      Dead state currentPage giữ nguyên. User verify tạm: employees row + auth user + cred file
      đã xoá sạch (kiểm count=0). Còn treo cùng họ: getReactionCounts + đếm tim admin chưa
      phân trang (trần 1000 dòng); chế độ lọc "tim" .in('id',...) vỡ ở ~400 id (header 16KB).

  - id: T-20260808-typed-client-gallery-4a
    title: "ADR-013 Phase 4a — gắn generic Database cho gallery (nhánh withAuth) + vá job tracking Drive"
    status: done
    owner: claude
    locks: ["app/actions/gallery-{admin,album,composite,dimensions,drive,image-helpers,selection}-actions.ts", "app/actions/blurhash-actions.ts", "types/gallery.ts"]
    spec: agent/DECISIONS.md ADR-013
    result: >
      8 file action gallery khai SupabaseClient<Database> (42 callback). 16 lỗi → 0.
      Verify: tsc 0 · eslint 0 · build pass.
    notes: >
      **BUG THẬT — tracking job "Lọc ảnh vào Drive" chết từ ngày đầu.** Bảng gallery_filter_jobs
      thật (migration 20260520090100) có folder_id/folder_name/total_files/copied_files; code
      (cả insert ở gallery-drive-actions lẫn gallery-selection-actions, update finalize, select
      progress, và interface GalleryFilterJob) viết theo một schema TƯỞNG TƯỢNG
      (job_type/total_count/processed_count/batch_id/…). Insert PGRST204 bị nuốt (error không
      destructure) → jobId luôn undefined → finalize không bao giờ chạy → bảng 0 dòng. Chức năng
      CHÉP Drive vẫn chạy được nhờ lỗi bị nuốt — chỉ tracking/lịch sử là chết.
      ĐÃ SỬA: insert/finalize/progress viết theo cột thật (folder_id=destFolderId, total_files,
      copied_files, error_log jsonb khi có fail) + log error thay vì nuốt; interface
      GalleryFilterJob viết lại theo schema thật. createGalleryFilterJob (0 nơi gọi, schema batch
      không thể map sang bảng thật vì folder_id NOT NULL) → giữ hàm nhưng throw tường minh với
      lý do (hành vi cũ cũng luôn throw, chỉ khác message PGRST204 khó hiểu).
      **BUG TIỀM ẨN:** galleries.allow_comments/allow_download NOT NULL nhưng
      GallerySettingsPayload cho phép null → truyền null là 23502; đổi guard !== undefined thành
      != null. set_gallery_password p_password không DEFAULT → cast (…|| null) as string như
      pattern lab-mutations. GalleryAlbum.sort_order/created_at khai đúng nullable theo DB.
      CHƯA LÀM (4b): gallery-core.ts còn 6 tham số supabase:any; gallery-public-actions +
      gallery-reaction-actions còn 15 chỗ createAdminClient() trần (đường công khai không qua
      withAuth) — cần verify render cả 2 mặt tiền khi làm.

  - id: T-20260808-no-3-mon
    title: "Trả 3 món nợ: sửa phát sinh kho (A) · trần 1000 dòng + 16KB gallery (B)"
    status: done
    owner: claude
    locks: ["supabase/migrations/20260808*.sql", "components/inventory/order-details-drawer.tsx", "app/actions/gallery-{core,reaction-actions,admin-actions,image-helpers,drive-actions}.ts"]
    spec: agent/HANDOFFS/T-20260808-no-3-mon.spec.md
    result: >
      A (commit riêng): tính năng phát sinh kho chưa từng chạy end-to-end (prod 0 phát sinh) —
      e2e add→update→delete với dữ liệu vứt bỏ moi ra 3 bug RPC: update ghi cột không tồn tại
      (inventory_transactions.updated_at/by + payments.updated_by), add nhánh bán lẻ truyền
      category_id='' vào cột uuid (22P02), delete xoá receipts trước txn con còn FK (23503).
      3 migration 20260808{100000,110000,120000} đã apply; e2e re-run PASS từng bước
      (stock 10→8→7→10, receipt 2000→4500→xoá, dọn 0/0/0). UI: nút Sửa hết stub — form 2 ô
      prefill, validate, payload thật.
      B (commit riêng): helper selectAllRows (phân trang .range + .order(id) bắt buộc) áp cho
      getReactionCounts, đếm tim admin, getCachedGalleryImages, initDriveCopyJob; drive copy bỏ
      .in()/.or() → lọc Set trong JS. Chứng minh pageSize=50 GIỐNG TỪNG PHẦN TỬ query 1 phát
      (501 ảnh + 116 reaction); 3 mode lọc khớp DB (0/95/95); render public dev = 501·116.
    notes: >
      Số bug "chưa từng chạy được" của riêng tính năng phát sinh giờ là 4 (payload rỗng UI +
      3 RPC). Mẫu quen: code/RPC viết theo schema tưởng tượng, chưa ai bấm nên chưa ai biết.
      Chưa verify UI click-through form Sửa (chỉ e2e tầng RPC + tsc/eslint; payload key khớp
      guard đã đọc đối chiếu tay). Drive copy thật vẫn chưa chạy (tạo folder Drive khách).
      getGalleryMetadataAll (fallback khi v3 vắng) vẫn select 1 phát — cố ý bỏ qua, v3 đang có.

  - id: T-20260808-typed-client-complete
    title: "ADR-013 HOÀN TẤT — Phase 4b + Phase 5 + khoá auth_utils/server"
    status: done
    owner: claude
    locks: ["app/actions/**", "lib/auth_utils.ts", "lib/supabase/server.ts", "lib/audit.ts", "lib/services/addon-sync-service.ts", "types/{gallery,settings,finance-operations}.ts"]
    spec: agent/DECISIONS.md ADR-013
    result: >
      4b (d588953): gallery-core 18 tham số any + 20 createAdminClient trần → typed; render 2 mặt
      tiền OK (admin 95/501, public 501·116). Phase 5 + khoá (852c97f): 38 file action (156
      callback) + createServerClient<Database> + 39 tham chiếu auth_utils — TOÀN BỘ server action
      giờ typed, không lùi được. 74+18 lỗi → 0. Verify: tsc 0 · eslint 0 · build · 11/11
      verify:<module> pass.
    notes: >
      4 bug thật đợt cuối: (1) Moodie duyệt sync Google Calendar select 4 cột ảo trên schedules
      (title nằm ở event_type, notes là mô tả) — luôn throw, đã viết lại payload theo đường chuẩn;
      (2) xoá bảng lương select employee_salaries.employee_name không tồn tại — luôn "Không tìm
      thấy bản ghi lương"; (3) join contracts nhiều-về-một trả OBJECT nhưng code đọc [0] — mã HĐ
      trong cảnh báo lương luôn "Không mã"; (4) schedule-actions.ts (0 nơi gọi) ghi
      schedule_date/color thay vì event_date/color_id.
      Tổng kết ADR-013 toàn chiến dịch: ~193 lỗi type → 0, moi ra 15 bug thật kiểu "schema tưởng
      tượng + lỗi nuốt". Ghi nhận thêm: notification_preferences boolean NULLABLE → helper
      mergeNotificationPreferences trong types/settings.ts.
