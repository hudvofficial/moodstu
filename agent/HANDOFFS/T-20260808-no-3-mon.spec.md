# T-20260808 — Trả 3 món nợ: sửa phát sinh kho · đếm tim vượt 1000 · lọc tim vượt 400

**Owner:** claude (fallback) · **Trạng thái:** chờ user duyệt
**2 task độc lập, locks không chồng nhau** — làm và commit riêng từng task.

---

## TASK A — "Sửa phát sinh" kho (module inventory)

**Locks:** `supabase/migrations/20260808*_fix_update_fulfillment_rpc.sql`, `components/inventory/order-details-drawer.tsx`

### A.0 Hiện trạng đo được — hỏng ở CẢ BA tầng

1. **UI:** nút Sửa ([order-details-drawer.tsx:318](components/inventory/order-details-drawer.tsx#L318)) chỉ `toast.info('Tính năng chỉnh sửa phát sinh đang được hoàn thiện, vui lòng xoá tạo lại nếu cần')` — không nối vào đâu. Modal `UnifiedModal` đã có title "Cập nhật phát sinh" nhưng không có input số lượng/đơn giá; đường `update_fulfillment` gửi payload `{}` (comment tự nhận "MVP placeholder").
2. **Action:** đã vá guard ở Phase 3 — payload thiếu thì báo "Thiếu số lượng / đơn giá để sửa phát sinh."
3. **RPC `update_fulfillment_transaction_atomic`:** đọc `pg_get_functiondef` + đối chiếu `information_schema.columns`:
   - `UPDATE inventory_transactions SET … updated_at = now(), updated_by = p_user_id` — bảng **không có cả hai cột** → RAISE, rollback toàn transaction, **mọi lời gọi hợp lệ đều fail**.
   - `UPDATE payments SET amount, updated_at, **updated_by**` — `payments` **không có `updated_by`** (chỉ có `updated_at`).
   - Các bảng khác RPC ghi (`contracts`, `contract_items`, `receipts`, `inventory_items`) — đã kiểm, đủ cột.
   - `delete_fulfillment_transaction_atomic` đã kiểm từng câu UPDATE/DELETE — **sạch**, đang hoạt động.

### A.1 Quyết định cần user chốt: làm form (khuyến nghị) hay gỡ nút?

- **A — Làm form (khuyến nghị):** toàn bộ hạ tầng server đã xây xong và không tồi — RPC atomic khoá `FOR UPDATE`, tự điều chỉnh tồn kho + payment + contract totals + receipt theo `source_type`, có nhánh duyệt cho sale/staff. Thiếu đúng: 2 dòng SQL sai cột + 2 ô input. Workaround "xoá tạo lại" mà toast gợi ý là thao tác 2 bước dễ quên tạo lại.
- **B — Gỡ nút:** xoá button Edit (~8 dòng), giữ nguyên RPC hỏng. Rẻ nhất nhưng bỏ phí hạ tầng đã xây và để lại RPC hỏng làm mìn cho người sau.

Phần dưới viết cho phương án **A**. Chọn B thì chỉ còn bước xoá button + ghi chú RPC hỏng vào vault.

### A.2 Task A1 — Migration sửa RPC

File mới `supabase/migrations/20260808100000_fix_update_fulfillment_rpc_columns.sql` — chép nguyên hàm hiện tại (từ `pg_get_functiondef`), chỉ đổi 2 chỗ:

```sql
-- Sửa update_fulfillment_transaction_atomic: bỏ các cột KHÔNG tồn tại.
-- (1) inventory_transactions không có updated_at/updated_by → câu UPDATE cuối RAISE
--     42703 → rollback → "sửa phát sinh" chưa từng chạy nổi tới cùng.
-- (2) payments không có updated_by (chỉ có updated_at).
-- Đối chiếu: delete_fulfillment_transaction_atomic không dính vì chỉ ghi
-- updated_at/updated_by vào contracts + inventory_items (đủ cột).

CREATE OR REPLACE FUNCTION public.update_fulfillment_transaction_atomic(p_txn_id uuid, p_new_quantity integer, p_new_unit_price numeric, p_user_id uuid)
 RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public'
AS $function$
-- [chép nguyên phần DECLARE + BEGIN từ pg_get_functiondef hiện tại, với đúng 2 sửa:]
--   UPDATE public.payments SET amount = v_new_sale_total, updated_at = now()   -- bỏ updated_by
--   UPDATE public.inventory_transactions SET quantity = p_new_quantity,
--          sale_unit_price = p_new_unit_price, sale_total = v_new_sale_total   -- bỏ updated_at + updated_by
$function$;
```

> Lúc thực thi: lấy nguyên văn bằng `node scripts/db-q.mjs "SELECT pg_get_functiondef(...)"` rồi áp 2 sửa — KHÔNG gõ lại tay. Apply bằng `node scripts/apply-migration.mjs <file>`.

**Verify A1:** gọi RPC với uuid giả → vẫn `"Không tìm thấy giao dịch"` (chữ ký ổn); diff `pg_get_functiondef` mới không còn chuỗi `updated_by` trong 2 câu UPDATE đó.

### A.3 Task A2 — UI form trong `order-details-drawer.tsx`

1. **State** (cạnh `reason`, dòng ~52) — string state theo bài học `Number("")===0`:
```tsx
  const [editQty, setEditQty] = useState("");
  const [editPrice, setEditPrice] = useState("");
```
2. **Nút Sửa** (dòng ~318) — thay `onClick` toast bằng:
```tsx
  onClick={() => {
    setEditQty(String(f.quantity ?? ""));
    setEditPrice(String(f.sale_unit_price ?? ""));
    setActionItem({ id: f.id, type: "update_fulfillment" });
  }}
```
3. **Modal** — trong `UnifiedModal` (dòng ~456), thêm khối trước phần lý do:
```tsx
  {actionItem?.type === "update_fulfillment" && (
    <div className="grid grid-cols-2 gap-3">
      <div className="space-y-2">
        <label className="text-body-sm font-medium text-text-primary">Số lượng</label>
        <Input type="number" inputMode="numeric" min={1} value={editQty}
          onChange={(e) => setEditQty(e.target.value)} placeholder="0" />
      </div>
      <div className="space-y-2">
        <label className="text-body-sm font-medium text-text-primary">Đơn giá</label>
        <Input type="number" inputMode="numeric" min={0} value={editPrice}
          onChange={(e) => setEditPrice(e.target.value)} placeholder="0" />
      </div>
    </div>
  )}
```
4. **`handleAction`** (dòng ~128) — payload thật + chặn sớm:
```tsx
  if (actionItem.type === "update_fulfillment") {
    const qty = Number(editQty); const price = Number(editPrice);
    if (!Number.isInteger(qty) || qty <= 0 || !Number.isFinite(price) || price < 0) {
      toast.error("Số lượng phải > 0 và đơn giá không âm."); return;
    }
  }
  // … chỗ payload:
  payload: actionItem.type === "update_fulfillment"
    ? { quantity: Number(editQty), sale_unit_price: Number(editPrice) }
    : undefined
```
5. Đóng modal → reset `editQty`/`editPrice` về `""` (trong `onClose` + sau success).

**Ghi nhận, KHÔNG sửa:** nút Xoá có `if/else` hai nhánh giống hệt nhau (dòng ~327-331) — dead conditional, để nguyên.

### A.4 Verify Task A (end-to-end trên dev local, dữ liệu tạm tự dọn)

1. `npx tsc --noEmit` 0 lỗi · `npx eslint <2 file đổi>` exit 0 · `npm run build` pass.
2. Dev server + login user verify tạm (đúng quy trình đã dùng cho fix lưới ảnh: tạo qua service role, UPDATE employees role admin, **xoá sạch sau khi đo**).
3. Trên UI: tạo 1 phát sinh bán lẻ trên vật tư thật (số lượng 1, giá 1.000) → bấm **Sửa** → đổi 2→2.000 → lưu → kiểm: dòng phát sinh đổi số, tồn kho giảm thêm 1, phiếu thu `receipts.receipt_amount` = 4.000.
4. Dọn: xoá phát sinh vừa tạo bằng nút Xoá (luồng delete đã hoạt động, tự hồi tồn kho + xoá receipt) → kiểm tồn kho về như cũ.
5. `npm run verify:inventory` pass.

---

## TASK B — Gallery: trần 1000 dòng + trần 16KB (module gallery)

**Locks:** `app/actions/gallery-core.ts` (additive), `app/actions/gallery-reaction-actions.ts`, `app/actions/gallery-admin-actions.ts`, `app/actions/gallery-image-helpers.ts`, `app/actions/gallery-drive-actions.ts`

### B.0 Hiện trạng — 4 điểm dính trần, không phải 2

| # | Chỗ | Trần | Ngưỡng vỡ | Hiện tại |
|---|---|---|---|---|
| 1 | `getReactionCounts` ([gallery-reaction-actions.ts:108](app/actions/gallery-reaction-actions.ts#L108)) — đếm tim trang KHÁCH | 1000 dòng | >1000 lượt reaction/gallery | max 116 |
| 2 | Đếm tim admin ([gallery-admin-actions.ts:~410](app/actions/gallery-admin-actions.ts#L410)) — `.in("gallery_id", …)` gộp MỌI gallery của hợp đồng | 1000 dòng | >1000 lượt tim/hợp đồng | 1.228 lượt toàn hệ |
| 3 | `getCachedGalleryImages` ([gallery-image-helpers.ts:58](app/actions/gallery-image-helpers.ts#L58)) — nguồn cho Download Manager + danh sách modal lọc | 1000 dòng | >1000 ảnh/gallery | **max 780 = 78% trần** |
| 4 | `initDriveCopyJob` ([gallery-drive-actions.ts:~300](app/actions/gallery-drive-actions.ts#L300)) — query ảnh 1 lần (trần 1000) + chế độ hearted `.in("id",…)` / both `.or(id.in.(…))` (16KB) | cả hai | ~400 ảnh tim (đo: 500 id = 19.670 ký tự) | 95 ảnh tim |

Không đụng: `getGalleryMetadataAll` (chỉ là fallback khi RPC v3 vắng — v3 đang có trên prod); `get_gallery_data_v3` (đếm trong SQL, không trần).

### B.1 Task B1 — helper `selectAllRows` trong `gallery-core.ts` (additive)

Grep xác nhận chưa có util tương tự. Thêm vào cuối `app/actions/gallery-core.ts`:

```ts
/**
 * Lấy TOÀN BỘ dòng của một query, vượt trần 1000 dòng/request của PostgREST
 * (supabase-js cắt ở 1000 IM LẶNG kể cả khi .limit() lớn hơn).
 * buildQuery PHẢI tạo builder MỚI mỗi lần gọi (builder không tái dùng được sau .range()),
 * và query PHẢI có .order() ổn định (thêm .order("id") làm tiebreaker) — không thì
 * phân trang không xác định, có thể sót/trùng dòng giữa các trang.
 */
export async function selectAllRows<T>(
  buildQuery: (from: number, to: number) => PromiseLike<{ data: T[] | null; error: { message: string } | null }>,
  pageSize = 1000,
): Promise<T[]> {
  const rows: T[] = [];
  for (let from = 0; ; from += pageSize) {
    const { data, error } = await buildQuery(from, from + pageSize - 1);
    if (error) throw new Error(error.message);
    const batch = data ?? [];
    rows.push(...batch);
    if (batch.length < pageSize) break;
  }
  return rows;
}
```

### B.2 Task B2 — `getReactionCounts` (điểm 1)

Thay khối select dòng 108-114 bằng:
```ts
    const data = await selectAllRows<{ image_id: string; reaction_type: string }>((from, to) =>
      supabase
        .from("gallery_reactions")
        .select("image_id, reaction_type")
        .eq("gallery_id", galleryId)
        .order("id")
        .range(from, to),
    );
```
(giữ nguyên vòng đếm phía dưới; bỏ nhánh `if (error)` vì helper đã throw — catch ngoài cùng của hàm giữ nguyên hành vi trả `{}`).

### B.3 Task B3 — đếm tim admin (điểm 2)

Thay khối `heartRows` bằng:
```ts
    const heartRows = await selectAllRows<{ gallery_id: string }>((from, to) =>
      supabase
        .from("gallery_reactions")
        .select("gallery_id")
        .in("gallery_id", galleryIds)
        .eq("reaction_type", "heart")
        .order("id")
        .range(from, to),
    );
```
(`galleryIds` là gallery của MỘT hợp đồng — vài phần tử, không dính 16KB.)

### B.4 Task B4 — `getCachedGalleryImages` (điểm 3)

Trong fetcher của `unstable_cache`, thay select đơn bằng `selectAllRows` giữ nguyên cột + order, thêm tiebreaker:
```ts
      const data = await selectAllRows<…cùng shape cũ…>((from, to) =>
        supabase
          .from("gallery_images")
          .select("id, file_name, drive_file_id, is_selected, selected_at, sort_order")
          .eq("gallery_id", id)
          .order("sort_order", { ascending: true })
          .order("created_at", { ascending: true })
          .order("id")
          .range(from, to),
      );
      return data;
```
(giữ nguyên tính chất "throw thay vì trả []" — helper throw sẵn, khớp comment chống cache-rỗng-2h.)

### B.5 Task B5 — `initDriveCopyJob` (điểm 4): bỏ hẳn `.in()`/`.or()`, lọc bằng Set trong JS

Pattern này **đã có tiền lệ ngay trong repo**: `getAllHeartedImagesForAction` (gallery-image-helpers.ts:105) lọc bằng `Set` trong JS — không URL dài, không trần.

1. Query reactions (dòng ~287) → bọc `selectAllRows` + `.order("id")`.
2. Thay trọn khối build `imagesQuery` + 3 nhánh filter + `heartedIdList` + `NO_MATCH_UUID` bằng:
```ts
    const allImages = await selectAllRows<{ id: string; drive_file_id: string | null; file_name: string | null; sort_order: number | null; created_at: string | null; is_selected: boolean | null }>((from, to) =>
      supabase
        .from("gallery_images")
        .select("id, drive_file_id, file_name, sort_order, created_at, is_selected")
        .eq("gallery_id", galleryId)
        .order("sort_order", { ascending: true })
        .order("created_at", { ascending: true })
        .order("id")
        .range(from, to),
    );

    // Lọc trong JS bằng Set — .in("id", ~400+ id) làm URL vượt 16KB (đo được:
    // 500 id = 19.670 ký tự → "HTTP headers exceeded server limits").
    const heartedSet = new Set(reactionHeartedIds);
    const images = allImages.filter((img) =>
      filterMode === "selected" ? !!img.is_selected
      : filterMode === "hearted" ? heartedSet.has(img.id)
      : !!img.is_selected || heartedSet.has(img.id),
    );
```
3. Giữ nguyên toàn bộ phía dưới (check rỗng, lọc JPG, tạo folder, tạo job).

### B.6 Verify Task B

1. `npx tsc --noEmit` 0 · eslint 5 file đổi exit 0 · `npm run build` pass.
2. **Chứng minh phân trang đúng bằng dữ liệu thật** — script scratchpad ép `pageSize=50` trên gallery CD Kim Chuẩn (501 ảnh/116 reaction): kết quả `selectAllRows` phải **bằng từng phần tử** với query 1 phát hiện tại (so sánh sorted JSON). Đây là bằng chứng vòng lặp không sót/không trùng.
3. Render dev local (login user verify tạm, dọn sau):
   - Admin gallery: chip Thả tim vẫn **95**; heartCount tab gallery không đổi.
   - Public gallery (`/gallery/[accessUrl]`): tổng tim vẫn **116**.
   - Modal "Lọc ảnh": 3 chế độ hiện đúng số như trước (tim 95 · chọn/cả hai như số hiện hành).
4. **KHÔNG chạy Drive copy thật** (tạo folder trên Drive khách) — verify tới `initDriveCopyJob` trả `filesToCopy` đúng số lượng bằng cách gọi action trực tiếp trong script với 3 mode rồi so số với DB (không bấm nút copy trong UI).
5. Sau deploy: mở lại admin + public gallery trên prod, số không đổi.

---

## Thứ tự & câu hỏi

1. User chốt **A hay B** cho "sửa phát sinh" (spec viết sẵn cho A).
2. Task B không chờ quyết định — có thể làm trước.
3. Commit riêng: `fix(inventory): …` và `fix(gallery): …`.
