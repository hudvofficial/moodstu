# CURRENT_STATE.md — Trạng thái thật (mood-studio)

> **File sống — Claude cập nhật mỗi phiên.** Đây là "sự thật hiện tại", thay cho các
> PLAN cũ đã lỗi thời (`plans/260603-native-feel-performance/` KHÔNG còn phản ánh
> thực trạng — user xác nhận đã tối ưu nhiều mà không cập nhật file đó).
> Cập nhật gần nhất: **2026-08-25** · nhánh: `main` @ `b42b74b`.

## 2026-08-26 — M1 SỔ TIỀN RA: **MERGED `d74d3cc` + VERIFIED PRODUCTION 4/4** (ADR-016 đợt 1 xong)
- User "merge + push luôn" → ff `main` `5b0183d → d74d3cc`, Vercel `success`, Playwright `cashflow-m1.spec.ts` **4/4 PASS trên `stu.moodwedding.com`** (seed riêng dọn sạch; sau test: `expenses` active vẫn 35, lab còn 1.905.000, integrity 0). Cửa sổ lệch DB↔app đã đóng. M2 tiếp theo: dashboard 3 số (Két / Lãi-lỗ / Công nợ) + lãi/lỗ tháng theo luật ngày + màn công nợ hợp nhất + drop 2 bảng `_legacy`/4 view.
- User gật ("nếu được cover rồi thì cho phép bạn Làm đi") → Claude fallback. Migration `20260825200000_cashflow_m1_expense_allocations.sql` (52 KB: schema `expenses.payee_type/payee_id/legacy_*`, bảng `expense_allocations`, `vendors.vendor_type`, `inventory_items.supplier_id`, category `vat_tu`; di trú 26 lab_payments + 5 vendor_payments + 4 lô phôi → 35 phiếu chi thật; xoá mềm 43 trích trước; 4 bảng cũ → `_legacy` + VIEW; drop 2 hàm trích trước + trigger; 3 RPC đơn in bỏ phiếu chi; hàm mới `payable_remaining/payable_items/recompute_printing_payment_status/record_payee_payment_atomic/finance_payable_summary/contract_financials` + 4 wrapper giữ chữ ký; `printing_integrity_report` 4 check mới; `inventory_stock_in_atomic` chữ ký mới tạo phiếu chi khi đã trả; REVOKE anon/authenticated kể cả `get_contract_list_v2` vốn đang mở) **ĐÃ ÁP production** — đối soát khớp 100% dự đoán (lab còn 1.905.000, vendor/supplier 0, `da_thanh_toan` 26=26, profit 61/62 khớp + HĐ-2026-0064 −1.350.000, integrity 4/4 = 0).
- App: 15 file (lab schema nhận `payment_date` — vá bug bỏ rơi ngày; lab/vendor history đọc view không embed; `voidVendorPayment` xoá mềm `expenses`; bỏ khối `upsert_vendor_expense` ở work-task; lương gắn `payee_type='employee'`; `createExpenseSchema` + `payee_*`; `deleteExpense` recompute payment_status; `stockIn` + form nhập kho có NCC/"Đã trả"/phương thức/ngày; `getContractFinanceDetails` theo `contract_financials` (mọi task không hủy + COGS + expenses `other`); xoá `contract-profit.ts` + fallback profit; `getActiveVendors` lọc `tho_ngoai`; `getSupplierOptions` mới). Vault 5 file viết lại. Phát hiện khi implement: `recordVendorPayment` gửi allocations `JSON.stringify` → RPC cũ coi là FIFO (bug có sẵn) → RPC mới tự parse string.
- Gate: tsc 0, eslint 0, build 0, verify:printing/contracts/inventory 3/3, **Playwright `tests/e2e/cashflow-m1.spec.ts` 4/4** (5 luồng RPC + smoke 5 màn; `/contracts` HĐ seed hiện +3.500.000 / chi phí 1.500.000 đúng công thức), seed dọn sạch. Chi tiết: spec §6. **Cần user merge + push sớm** (DB đã mô hình mới; app prod cũ chỉ vỡ "huỷ thanh toán thợ" tới khi deploy).

## 2026-08-25 (tiếp #11) — ADR-016 ACCEPTED (user uỷ quyền) + luật ngày ghi sổ + spec M1 sổ tiền ra (CHỜ GẬT ĐỂ CODE)
- User: *"chuyện tháng ký là đợt rồi mình bỏ bê hệ thống không update kịp… vấn đề của bạn là thiết kế đường đi dữ liệu đúng là được"* → uỷ quyền chính sách cho Claude. Đo: 33 HĐ hoàn thành chỉ 15 bấm cùng tháng ngày chụp, 8 bấm muộn >30 ngày; 22/26 lần trả lab nhập cùng ngày 25/08 — **bug có sẵn:** modal trả lab có ô ngày nhưng `labPaymentSchema` bỏ rơi `payment_date` + bảng `lab_payments` không có cột ngày; `work_tasks.completion_date = now()` lúc bấm. → **Luật ngày (ADR-016 §2, design §3.1b):** doanh thu theo `contracts.work_date` (ngày chụp, fallback `contract_date`, loại `da_huy`, KHÔNG theo status/updated_at); chi phí task theo `contract_events.event_date` khi hoàn thành; đơn in theo `order_date`; thu/chi theo ngày nhập trên phiếu. Câu "tháng ký hay hoàn thành" biến mất: là **tháng chụp**.
- **ADR-016 ghi vào DECISIONS.md, Accepted.** Phát hiện thêm khi đọc RPC: `create/update_printing_order_atomic` gọi `upsert_printing_expense` bên trong, `delete_printing_order_atomic` xoá mềm expense; `printing_integrity_report` có 5/7 check dựa trên phiếu chi trích trước → M1 phải viết lại cả 4 RPC này. `payEmployeeSalaryAction` đã tạo phiếu chi khi trả (đúng) nhưng không có payee/category.
- **Spec M1:** `agent/HANDOFFS/T-20260825-cashflow-m1-so-tien-ra.spec.md` — migration `20260825200000` (schema `expenses.payee_type/payee_id`, bảng `expense_allocations`, `vendors.vendor_type`, `inventory_items.supplier_id`, category `vat_tu`; di trú 26 lab_payments + 5 vendor_payments + 4 lô phôi → 35 phiếu chi thật với DO-block tự kiểm; xoá mềm 43 trích trước; 4 bảng cũ → `_legacy` + **view tương thích** để app đọc không đổi; bỏ trigger/2 hàm trích trước; 3 RPC đơn in bỏ phiếu chi; hàm mới `payable_remaining`, `payable_items`, `recompute_printing_payment_status`, `record_payee_payment_atomic` + 2 wrapper giữ chữ ký, `finance_payable_summary` + 2 wrapper, `contract_financials(uuid[])` thay 4 bản sao, `printing_integrity_report` 4 check mới, `inventory_stock_in_atomic` chữ ký mới tạo phiếu chi khi "đã trả"). App diff nhỏ (10 file). Verify §4 có snapshot before/after: profit 59/60 khớp, HĐ-2026-0064 −1.350.000 (task vendor đang làm giờ là cam kết), lab còn 1.905.000, vendor/supplier 0. **Chờ user gật là code (Claude fallback).**

## 2026-08-25 (tiếp #10) — THIẾT KẾ DÒNG TIỀN v2 "Ba sổ, một hợp đồng" — đề xuất kiến trúc, CHỜ USER CHỐT 4 ĐIỂM
- User trả lời 4 câu của báo cáo #8: (1) thiệp cưới là **mảng kinh doanh lẻ riêng**, khách HĐ có nhu cầu thì kèm HĐ; (2) **không tồn kho** — mọi thứ ảnh đẩy qua lab; (3) ekip tính chi phí **theo hợp đồng**, chưa có lương tháng (sau sẽ có → thiết kế sẵn); (4) **dòng tiền rối nhất** — cần thiết kế lại thu/chi/báo cáo.
- Đo thêm: `upsert_vendor_expense` chỉ trích trước khi task `hoan_thanh` → task 1,35M "đang làm" thiếu phiếu chi là ĐÚNG luật; cả 43 phiếu chi trích trước ghi `payment_method='chuyen_khoan'` dù lab còn nợ 1,9M; `finance_dashboard_metrics.profit = (payments + receipts) − expenses` = tiền vào két − cam kết; `finance_revenue_by_month` = tiền thu; **lương ĐÃ đúng** (`payEmployeeSalaryAction` tạo phiếu chi khi trả, không trích trước); enum `transaction_type` có sẵn `hoa_don` (0/62 dùng); `employee_salaries` có `product_salary` (= Σ `work_tasks.cost`) + `base_salary` (1 dòng test 100.000.000).
- **Gốc rối:** bảng `expenses` ("phiếu chi" = tiền ra két) bị dùng làm sổ cam kết cho lab + thợ ngoài, trong khi lương dùng đúng → 3 nhánh 2 nghĩa. **Thiết kế:** `docs/design/dong-tien-mood-v2.md` + artifact "Ba Sổ Một Hợp Đồng". Ba sổ: CAM KẾT (không phải bảng — `contracts.total_amount`, `work_tasks.cost` khi hoàn thành, `printing_orders.total_amount`, overhead), TIỀN VÀO (`payments` luôn có `contract_id`; bán lẻ = HĐ `hoa_don`; `receipts` nghỉ hưu), TIỀN RA (`expenses` = phiếu chi thật + `payee_type/payee_id` + bảng mới `expense_allocations` thay `lab_payments`/`vendor_payments`/2 bảng phân bổ; bỏ `upsert_printing_expense`/`upsert_vendor_expense`/trigger). Thiệp = đơn đặt in tới `labs` (`lab_type` ảnh/thiệp), kho đóng băng. Nhân sự: `work_tasks.cost` (HĐ) *hoặc* lương cứng (overhead), cả hai trả bằng phiếu chi lương → không trùng. 6 báo cáo có công thức; dashboard 3 số (Két / Lãi-lỗ / Công nợ). Di trú có số đối soát (26 lab_payments 7.936.400 → phiếu chi; 5 vendor_payments 11.850.000; xoá mềm 43 trích trước; 4 phiếu thu thiệp → HĐ `hoa_don`). Lộ trình M1 sổ tiền ra → M2 dashboard → M3 thiệp → M4 tiền vào → M5 nhân sự.
- **Phát hiện khi đối chiếu với #9 (phiên song song):** công thức lợi nhuận HĐ giờ có **4 bản sao** (`finance_contract_profit_report`, `get_contract_list_v2` vừa thêm, `getContractFinanceDetails` vừa vá, `contract-profit.ts` dead) → M1 gom về 1 hàm SQL `contract_financials()`.
- **ĐÍNH CHÍNH (user chỉnh ngay sau đó):** Mood **tự nhập phôi thiệp lô lớn, tồn kho, tự in tại Mood theo đơn khách** — chỉ hình cưới mới đẩy lab. → Bản 1 thiết kế hiểu sai (thiệp = đặt in ngoài, đóng băng kho) — **đã sửa thành bản 2**: mã `HD527/HD513/HD394` là **mẫu phôi** (không phải mã HĐ); module kho là **xương sống mảng thiệp**, giữ + sửa 2 mũi tên tiền thiếu: (a) `stock_in` → **phải trả NCC phôi** (payee mới, `inventory_items.supplier_id → vendors` mở rộng "đối tác ngoài") + phiếu chi phân bổ lô; (b) **giá vốn `stock_out.total_cost` vào lãi/lỗ** tháng/HĐ/mảng thiệp. Bỏ ý `hoa_don`/nghỉ hưu `receipts` — pipeline `create_sale_receipt_atomic` đã đúng; khách HĐ mua thiệp đi "Bán thêm HĐ" (RPC có sẵn, 0 lần dùng). 4 phiếu thu bán lẻ là khách lẻ thật (SĐT không khớp khách HĐ). Đính chính đã ghi vào đầu `docs/reports/audit_2026-08-25_luong-du-lieu-nghiep-vu.md` + artifact audit (v2-erratum).
- **User trả lời lần 2:** (1) chưa hiểu mô hình → yêu cầu trace kĩ hướng dữ liệu nghiệp vụ rồi báo lại; (2) không hiểu "tháng ký"; (3) **CHỐT: phôi nhập về trả tiền ngay** (chưa ngâm công nợ); (4) **CHỐT: công in tính vào giá bán** (xuất ít). → Viết giải thích bằng 2 câu chuyện thật: **HĐ-2026-0009** (Đinh Thị Nga 3.500.000: 24/05 ký+cưới+cọc 500k+thợ 1,1M+đơn in 280k → 2 phiếu chi ảo; 10/06 tất toán 3M; **25/08 trả thợ+lab thật** không báo cáo nào đọc) và **lô phôi HD527** (02/05 nhập 1,5M trả ngay không phiếu chi; 12/05 bán 2.950 tờ 3.245.000, giá vốn 1.475.000 không vào lãi/lỗ). Bảng theo tháng: tổng 3 cách đều = 2.120.000 nhưng từng tháng lệch (T5 dashboard −880k vs két +500k; T8 chi thật 1,38M vs dashboard 0). Artifact "Một Hợp Đồng Đi Qua Ba Sổ" + design §11–§12. Thiết kế cập nhật: form nhập kho mặc định "Đã trả" → `inventory_stock_in_atomic` tạo phiếu chi supplier + allocation; bỏ task `in_thiep`. **Còn chờ:** gật câu 1 (3 sổ) + chọn câu 2 (tháng hoàn thành — đề xuất — hay tháng ký).

## 2026-08-25 (tiếp #9) — /contracts: thêm cột Chi phí/Lợi nhuận (gate theo quyền finance) — MERGED + PROD
- User gửi ảnh bảng `/contracts` (10 cột, thiếu Chi phí/Lợi nhuận): "table hiển thị thiếu thông tin: Chi phí và lợi nhuận / tối ưu UI dựng mình xem."
- Trace: `get_contract_list_v2` (RPC nuôi list) không có field cost/profit nào. **Phát hiện quyền hạn khi trace:** role `sale` có quyền `contracts` nhưng KHÔNG có `finance` — nếu thêm field vô điều kiện sẽ lộ số tài chính nội bộ qua Network response dù UI ẩn cột.
- **T-20260825-contracts-list-financials MERGED** (`b42b74b`, Claude fallback). Migration `20260825160000_contract_list_add_financials.sql`: thêm `total_cost/profit/profit_margin` vào RPC, copy nguyên công thức `finance_contract_profit_report` (loại `work_tasks.vendor_id IS NOT NULL`, loại `expenses [Auto-Print]%`). `contract-queries.ts`: role không có `finance` → server xóa hẳn 3 field khỏi payload (không chỉ ẩn UI). UI: đúng 1 cột mới "Lợi nhuận" (gộp Chi phí làm dòng phụ) desktop, dòng phụ thứ 3 trong ô Tổng cộng/Còn nợ ở tablet, dòng riêng ở mobile — chỉ render khi field có mặt trong data. Bấm mở lại `ContractProfitDetailDrawer` đã có ở `/finance/dashboard` (tái dùng, không viết lại breakdown).
- **Bug phát sinh khi verify (vá luôn cùng task):** đối chiếu số list ↔ drawer cho cùng HĐ lệch (+1.900.000 vs +550.000) → `getContractFinanceDetails` (nuôi chính drawer, dùng ở cả `/finance/dashboard`) thiếu filter `vendor_id IS NULL` trên `work_tasks`, đếm cả task vendor vào "Chi phí Lương" — **bug đang sống**, không phải out-of-scope, vá bằng đúng pattern đã chứng minh ở `finance_contract_profit_report`.
- Verify: eslint 0, tsc 0, build 0. Playwright local + **production** (seed 2 role admin/sale, xóa sạch): admin thấy cột đúng vị trí, số khớp RPC (1.900.000đ cho HĐ-2026-0064), bấm mở đúng drawer không lộ nhầm `ContractDrawer`, số khớp 100% sau khi vá vendor_id; sale không thấy cột, đúng 10 cột như ảnh gốc. Render OK desktop/tablet/mobile. Chi tiết: `agent/HANDOFFS/T-20260825-contracts-list-financials.spec.md`.

## 2026-08-25 (tiếp #8) — Rà soát KIẾN TRÚC DỮ LIỆU NGHIỆP VỤ toàn hệ thống ("kiến trúc sai tè le" — user) → báo cáo "Sổ nào là chân lý?"
- User yêu cầu trace rộng lại nghiệp vụ + quan hệ dữ liệu + đường đi đúng sau khi lộ ra thiệp cưới đi qua kho. Đo toàn bộ DB prod (chỉ đọc) + `pg_proc` 10 RPC tài chính + vault + code. Báo cáo: **`docs/reports/audit_2026-08-25_luong-du-lieu-nghiep-vu.md`** (số liệu thô ở phụ lục) + artifact "Sổ Nào Là Chân Lý".
- **Kết luận:** tầng kỹ thuật KHÔNG sai (hợp đồng trung tâm, RPC atomic, accrual lab/thợ ngoài nhất quán). Sai ở MÔ HÌNH NGHIỆP VỤ, 4 chỗ: (1) **3 loại "nhà cung cấp" 3 cách ghi sổ** — lab (`labs`/`lab_payments`), thợ ngoài (`vendors`/`vendor_payments`), "supplier" text trong kho → xưởng thiệp rơi vào loại 3, vô hình; (2) **thiệp**: 2.880.000đ vốn không phiếu chi, 5.295.000đ thu là phiếu bán lẻ không `contract_id` (SĐT không khớp khách nào); (3) **`expenses` "hội tụ" chỉ 2/6 dòng chảy** (In ấn 33 · 9.841.400, Thợ ngoài 10 · 11.850.000; lương/vật tư/cố định 0) và **0/10 RPC tài chính đọc `lab_payments`/`vendor_payments`** → "dòng tiền" = thu cash + chi cam kết; snapshot cộng lương, dashboard không → 2 số; (4) **catalog không có ruột**: 18 gói đều `single`, `cost_price` 0, bundles rỗng → không tự sinh đơn in/task/giá vốn → gốc rễ của việc thiệp "rơi".
- Phát hiện kèm: `contract-profit.ts` (app) đếm trùng chi phí vendor (task + expenses) nhưng **0 caller** — dead code bẫy; `getContractProfitReportFallback` là bản sao thứ 3; 1 task vendor (AC · trợ lý · 1.350.000 · HĐ-2026-0064, tạo 25/08, đang làm) chưa có phiếu chi accrual — cần chốt luật "trích khi nào"; `contract_checklists` 333 dòng = studio đang làm tay "ruột gói".
- **Đường đi đúng đề xuất:** Luật 1 (chi phí: 1 bản ghi gốc → 1 phiếu chi accrual có `contract_id` → 1 sổ công nợ → 1 hàm lợi nhuận), Luật 2 (doanh thu khách HĐ luôn có `contract_id`). Thiệp = **đơn đặt in** (`printing_orders`, `labs` = "nhà in" nói chung) — không bảng mới; kho chỉ cho hàng tồn thật + `inventory_stock_in_atomic` sinh phiếu chi; dài hạn gộp 3 đối tác thành 1; nói rõ cash vs cam kết; 1 hàm lợi nhuận; `service_bundles` làm ruột gói. Lộ trình P0/P1/P2 + **4 quyết định chờ user** (thiệp trong gói hay bán rời; có hàng tồn thật không; nhân sự theo task hay lương tháng; "dòng tiền" là cam kết hay cash). **Chưa viết spec code nào — chờ user chốt 4 câu.**

## 2026-08-25 (tiếp #7) — Hủy đơn in: trace nhánh "hoàn kho" → xác nhận dead code 100%, spec T-20260825-printing-cancel-inventory-deadcode (CHỜ DUYỆT)
- User yêu cầu "trace kĩ nhánh hoàn kho trong cancelOrder() rồi viết spec". Đo trực tiếp DB + code, kết luận: chuỗi kho ↔ đơn in **đứt ở cả 2 đầu**. Đầu vào: picker "Liên kết vật tư (tùy chọn)" trong drawer là UI câm từ đầu — `printingItemSchema` (zod) chỉ có name/quantity/unitPrice nên `item_id` bị strip trước khi tới RPC → **0/32 đơn** có item_id (đúng lý do ADR-014 ghi "reservation luôn rỗng"). Đầu ra: `startProduction/completeProduction` đã xoá ở ADR-014 → không nơi nào set `inventory_status` = reserved/stocked_out. DB: `inventory_reservations` **0 dòng từ khi tạo**, `inventory_transactions` source_type=printing_order **0**, reservation_id not null **0**; `inventory_status` 31 none / 1 cancelled (`IN-260615-00019`, soft-deleted — đơn duy nhất từng đi qua `cancelOrder()`). `CancelOrderModal.hasInventory` luôn false vì `mapPrintingOrderRow` không nạp `inventoryStatus`. Thêm: `check_inventory_conflict()` tham chiếu cột `inventory_item_id/start_date/end_date` không tồn tại (cột thật: item_id, order_id, reserved_quantity, expires_at…) → hỏng sẵn, 0 caller (váy dùng `is_dress_available`); `expire_old_reservations()` chưa từng lên lịch (pg_cron chưa cài).
- **Phát hiện kèm (cùng hàm):** 2 đường hủy đơn lệch side-effect — dropdown (`updatePrintingOrderStatus`) ghi `printing_order_status_history` (66 dòng) nhưng không ghi `cancelled_at/cancellation_reason`; modal (`cancelOrder`) ngược lại + không kiểm `PRINTING_VALID_TRANSITIONS` — đúng lớp "2 nơi định nghĩa 1 luật" ADR-015 vừa vá.
- **Spec:** `agent/HANDOFFS/T-20260825-printing-cancel-inventory-deadcode.spec.md` — Phase A (code, bắt buộc): xoá `cancelOrder()`/file workflow-mutations, `CancelOrderModal` gọi `updatePrintingOrderStatus` (ghi thêm `cancelled_at/cancellation_reason` khi `huy_don`), gỡ picker vật tư + `item_id` + 8 field/6 type chết ở `types/printing.ts`; Phase B (DB, tuỳ chọn — khuyến nghị): DROP `order_payment_summary`, `inventory_available_stock`, `expire_old_reservations()`, `check_inventory_conflict()`, `inventory_transactions.reservation_id`, `inventory_reservations`, `order_payments` (tất cả 0 dòng / 0 tham chiếu, đã đo pg_views/pg_proc). Đề xuất ADR-016. **CHƯA implement, chờ user duyệt.**
- **User hỏi tiếp: "HĐ trọn gói in thiệp cưới — đường data đó check chưa?"** → kiểm riêng (spec §1h). Thực tế: **thiệp cưới đi qua module Vật tư, không qua đơn in** — `inventory_items` có đúng 3 dòng, cả 3 là thiệp gắn mã HĐ (`VT-016 Thiệp Cưới - HD527`, `VT-017 … HD513`, `VT-018 Tân Gia - HD394`), 9 giao dịch: nhập kho khi in xong (vốn 380–500đ/tờ, tổng 2.880.000đ) → giao khách bằng **phiếu bán vật tư** `retail_sale` (4 phiếu 5.295.000đ) hoặc `internal_use`; `contract_id` NULL ở cả 9 dòng, mode "Xuất HĐ"/"Bán thêm HĐ" chưa từng dùng; `printing_orders` 0 thiệp, `contract_items` 0 thiệp (bảng không có cột link kho — FK `contract_items_inventory_item_id_fkey` thực chất là `dress_id → dresses`). 13 RPC kho không tham chiếu object bị drop → **spec không chạm đường thiệp**. Phát hiện kèm cho Tài chính (ngoài scope, chờ user xác nhận nghiệp vụ): giá vốn thiệp không vào lợi nhuận theo HĐ (COGS chỉ đếm `contract_fulfillment/contract_addon_sale`), 2.880.000đ vốn không có phiếu chi, khả năng đếm doanh thu 2 lần nếu thiệp đã trong gói.

## 2026-08-25 (tiếp #6) — /printing: review 2 vòng theo ảnh user → spec T-20260825-printing-drawer-fixes (6 bug, CHỜ DUYỆT)
- User gửi ảnh dropdown trạng thái đơn `IN-260609-00016` ("check kĩ drawer đang có nhiều bug") rồi hỏi tiếp "thao tác rồi phải hard F5 mới thấy update" → 2 vòng review liên tiếp (không code, đúng vai trò Claude), tìm 6 bug độc lập, tất cả nằm trong luồng đổi trạng thái đơn in mà ADR-014 (`T-20260824-printing-workflow-redesign`) mới thiết kế lại hôm qua:
  1. **P0 — reason bị bỏ sót:** server (`printing-mutations.ts`) bắt buộc `reason` cho `gap_su_co`/`huy_don`, nhưng `printing-list-page.tsx` (`/printing`, đúng UI trong ảnh) không thu thập reason ở đâu cả → chọn 2 trạng thái này từ dropdown **luôn fail**. Đối chứng `print-orders-block.tsx` (`/contracts/[id]`) tưởng đúng nhưng cũng thiếu — `requiresReason()` ở đó quên `huy_don`, nên "Hủy đơn" fail Ở CẢ 2 TRANG, không riêng /printing.
  2. **P0 — 2 drawer giữ snapshot đông cứng:** `selectedContractGroup`/`editingOrder` (`printing-list-page.tsx`) là `useState` chụp 1 lần lúc mở, không có cơ chế đồng bộ lại theo SWR — đúng nguyên nhân "phải F5 mới thấy" user hỏi. Cả `PrintingGroupDrawer` lẫn `PrintingDetailDrawer` (next-step trong drawer không tự đóng) đều dính.
  3. **P1 — badge "Thanh toán lab" đọc sai bảng:** `getOrderPaymentSummary` đọc view `order_payment_summary` ← `order_payments` (mô hình "khách cọc cho Mood", ADR-014 đã khai tử cho đơn in) thay vì `lab_payment_allocations` (Trục B thật) → luôn hiện đúng bằng tổng đơn gốc, không trừ phần đã trả lab.
  4. **P2 — "Hoàn tiền" trong CancelOrderModal chết vĩnh viễn + không còn cơ sở nghiệp vụ:** gate theo `order.paidAmount` không nơi nào set; hoá ra không phải quên nạp mà là tàn dư "đặt cọc" đã bị ADR-014 khai tử, sót lại vì file này không nằm trong `locks` của task đó.
  5. **P3 — overdue lệch múi giờ:** `new Date(expectedDate) < new Date()` parse UTC so với giờ local, lệch tới 7h quanh mốc đổi ngày (VN UTC+7), lặp lại y hệt ở 3 file.
  6. **P3 — `gap_su_co`/`huy_don` trùng màu đỏ** ở cả dot dropdown lẫn badge.
- **Phát hiện phụ:** spec gốc ADR-014 mục 3 (`isRollback` bỏ `dat_coc`/`da_giao` khỏi mảng `ORDER`) **chưa được áp dụng khi implement** — vẫn còn trong `printing-mutations.ts` (không gây sai hành vi vì 2 giá trị đó không còn transition nào chạm, nhưng lệch spec gốc). Sửa bookkeeping: `TASKS.yaml` vẫn ghi `T-20260824-printing-workflow-redesign` là `active/status:ci` dù `git log` xác nhận đã merged (`0f9a3cb`) từ hôm qua — đã chuyển sang `done:`.
- **Spec:** `agent/HANDOFFS/T-20260825-printing-drawer-fixes.spec.md` (12 mục + §13 kết quả). User duyệt "ok duyệt theo đề xuất, bạn trực tiếp tiến hành triển khai code" → **ADR-015 ghi vào DECISIONS.md (Accepted)**, Claude fallback implement trên branch `claude/printing-drawer-fixes` (Codex CLI vẫn 404 credential).
- **T-20260825-printing-drawer-fixes — MERGED + VERIFIED PRODUCTION** (`e42ff30` fix + `d91def7` docs, ff vào `main`, push 15:08Z theo lệnh user "tiến hành merge + push luôn"; Vercel `success` ~140s; **Playwright 3/3 PASS trên `stu.moodwedding.com`** — lượt 1 fail vì `networkidle` không đạt trên prod (Speed Insights/Realtime), đổi `goto` sang `domcontentloaded`; seed dọn sạch 0 sót/7 bảng). 14 file trong locks + 1 component mới (`status-reason-modal.tsx`, SSOT modal lý do cho cả `/printing` lẫn `/contracts/[id]`). Gate: eslint 0 error (1 warning pre-existing), tsc 0, build exit 0, `verify:contracts` xanh, **Playwright 3/3** trên `next start` prod :3100 (seed riêng 1 user/1 HĐ/1 lab/3 đơn, dọn sạch — 0 dòng sót/7 bảng): group drawer đang mở tự đổi trạng thái + header 0/3→1/3 không F5; modal lý do hiện đúng cho Gặp sự cố/Hủy đơn ở cả 2 trang; dot màu đo `rgb(243,156,18)` ≠ `rgb(231,76,60)`; badge nợ lab **262.500đ** (= 362.500 − 100.000 đã trả, trước fix hiện 362.500); next-step đổi badge tại chỗ; modal Hủy không còn "Hoàn tiền". Spec giữ lại làm regression: `tests/e2e/printing-drawer-fixes-verify.spec.ts`.
- **Lệch spec có lý do:** ADR-015(b) dùng `useMemo` derive (snapshot làm key) thay `useEffect`+`setState` — lint `react-hooks/set-state-in-effect` (React Compiler) là **error** trong repo, tsc không bắt. Thêm `setLabDebt(null)` ở effect reset drawer để số nợ đơn trước không lọt sang đơn mới trong lúc chờ fetch.
- **Phát hiện có sẵn trên prod qua gate `verify:printing` (ĐỎ):** `printing_stats()` (SECURITY DEFINER) **anon gọi được** — migration ADR-014 `20260824120000` DROP+CREATE hàm này nhưng không áp lại REVOKE/GRANT của `20260428130000:789/802`. Migration `supabase/migrations/20260825150000_printing_stats_revoke_anon.sql` (2 dòng) **ĐÃ ÁP prod** (user: "bạn áp luôn đi, mọi khi bạn vẫn tự áp migration luôn được mà") → `verify:printing` **xanh** (anon denied 4/4, integrity 7/7 = 0). Bài học: mọi migration DROP+CREATE hàm SECURITY DEFINER phải kèm lại REVOKE/GRANT; gate `verify:printing` bắt được nhưng T-20260824 không chạy nên lọt.

## 2026-08-25 (tiếp #5) — gallery khách: trace gốc rễ 2 "phát hiện có sẵn" → M3-A spec (tab ĐÃ CHỌN), M3-B kế, M3-C bỏ
- User hỏi "2 phát hiện đó trace kĩ chưa, cần đề xuất fix triệt để" → thừa nhận chưa (mới giả thuyết), đo lại production bằng log grid `[useMasonryGrid]` (prod không strip console) + timeline từng request/response phân trang:
  - **Gốc rễ tab ĐÃ CHỌN 40/70:** effect AUTO-TRIGGER `use-masonry-grid.ts:145` có dep `onLoadMore` — callback đổi identity khi `size`/`isValidating` đổi (hệ quả của chính việc gọi nó) → effect chạy 2 lần → `setSize` 2 lần → 2 request trang song song → 1 bị `net::ERR_ABORTED` → trang đó mất (vd trang 1: request 3.2s + 3.6s, abort 3.6s, page=2 về 3.9s). `loadingMoreImages = isValidating && pagesData.length === size` sai nghĩa (false lúc đang tải trang mới). Không có thử lại. Tab TẤT CẢ tự bù nhờ khách cuộn (đã kiểm: cuộn hết ra đủ **517/517**) nhưng **5 request bị hủy mỗi lượt mở album** (2G page 20 → ~26). Tab lọc ngắn không cuộn → chết. Giả thuyết dedupe 5s **sai** (bấm sau 12s vẫn kẹt).
  - **Lỗi console `/login`: hạ cấp — chỉ có ở local.** Script `/dca4b236a0ed44fb/script.js` (của `<SpeedInsights/>` `app/layout.tsx:157`, đường proxy Vercel) production trả `200 application/javascript`, **0** redirect; ở `next start` không có Vercel → `proxy.ts` matcher không loại → đẩy `/login` → MIME error. Không phải bug production.
- **Đề xuất đã duyệt ("A rồi B, bỏ C"):** M3-A tab ĐÃ CHỌN dùng action riêng `getPublicSelectedImagesFull` **lazy** (không mở rộng `getPublicSelectedImages` eager — +110KB cho mọi khách trên 2G); M3-B vá chuỗi phân trang (ref cho `onLoadMore`, chặn `setSize` trùng, sửa `loadingMoreImages`) với gate đo 5→0 request hủy; M3-C bỏ (dọn log local phải chạm `proxy.ts`/`layout.tsx`, sai tỉ lệ).
- **T-20260825-gallery-selected-tab MERGED** (`ee58286`, Claude fallback). Action `getPublicSelectedImagesFull` lazy + client. Verify: eslint 0, tsc 0, build 0, **Playwright 11/11 local** — Xuân Phúc bấm ĐÃ CHỌN ngay → **70/70** (trước fix 40), +1/+0 request, gallery E2E 172 ảnh: bỏ chọn #150 (ngoài trang đầu) → tile mất + DB false, chọn #0 ở TẤT CẢ → có mặt khi quay lại tab, nút Tải/thanh đáy không đổi, GHI CHÚ vẫn 126, Slow 3G thấy spinner. Test data đã xoá. **Production** (deploy 173s): **6/6 PASS** chỉ-đọc — Xuân Phúc 70/70 ngay khi bấm. Spec §6.
- **T-20260825-gallery-pagination-chain (M3-B) MERGED** (`f687cff`, Claude fallback) — thu hẹp còn **1 file** `public-gallery-client.tsx`: admin đã vá đúng lỗi này ở `use-gallery-data.ts:90-96` bằng ref (comment ghi rõ "2 nguồn kích... chốt bằng state không chặn được 2 lời gọi cùng tick... mất trọn trang 6"), phía khách chưa vá → làm theo tiền lệ, không đụng `use-masonry-grid.ts`. `requestedSizeRef` + `setSize(pagesData.length+1)` + `loadingMoreImages` đúng nghĩa. Verify local: probe cuộn-hết ×2 → **0 request trang trùng** (prod trước: trùng ngẫu nhiên p3/p5 hoặc p4), 0 trang không về, 517/517, **spinner Slow-3G true** (prod trước: false ×2); regression 6/6. **Thước đo chỉnh lại sau khi trace sâu:** POST `ERR_ABORTED` thô không phải gate — phần lớn là Next đóng RSC stream **sau khi đã nhận 200** (cả stats/reactions), dữ liệu vẫn vào grid; cái gây mất trang là request **trùng** — đó là thứ B chặn. **Production** (deploy 158s, probe ×3 + verify 6/6): 0 trang không về, 517/517, spinner Slow-3G true ×3, chuỗi `p0@100→p1→…→p5` mỗi trang 1 lần; 1/3 lần thấy `p0x2` (trang 0 fetch lúc mount, ngoài chốt setSize, nghi đổi `pageSize` theo mạng làm đổi key — có sẵn, không tái hiện, ghi nhận không kết luận).

## 2026-08-25 (tiếp #4) — gallery khách: M2 tab GHI CHÚ — MERGED · phát hiện tab ĐÃ CHỌN lọc thiếu + lỗi `/login` script có sẵn
- **T-20260825-gallery-noted-tab MERGED** (`334b947`, Claude fallback, user duyệt "triển khai đi bạn" sau khi hỏi "đủ cover chưa" → rà thêm ra 1 lỗ hổng thật: chớp "Chưa có ảnh nào" lúc fetch lần đầu → vá bằng `isLoading` + spinner). 2 file +123/−19. Verify: eslint 0, tsc 0, build 0, **Playwright 17/17 local**: gallery thật 126 tile = chip không phụ thuộc cuộn, +1 request lần đầu / +0 lần sau, @375 3 tab 1 hàng; gallery E2E 172 ảnh: ✓/❤️ trên ảnh #150 (ngoài trang đầu) ăn + DB đúng, sửa ghi chú viewer đứng yên, xoá ghi chú viewer lùi về ảnh cuối / đóng khi rỗng, Slow 3G thấy spinner không thấy trạng thái rỗng. Test data đã xoá (0 còn lại). **Production** (deploy 193s): 8/8 PASS chỉ-đọc; **AC6** probe tab ĐÃ CHỌN sau deploy y hệt trước (160 / 40 / 9) → tab cũ không đổi, bug 40/70 vẫn còn nguyên chờ M3.
- **Phát hiện có sẵn #2 (ngoài scope, chưa sửa):** console `Refused to execute script from '/login' (MIME text/html)` xuất hiện ngay khi **chỉ load** trang gallery công khai, cả gallery thật lẫn test, trước khi bấm gì — 1 request `<script>` từ trang gallery bị trả về trang `/login` (proxy/auth bắt nhầm URL script của trang công khai). Cần task riêng.
- Bẫy test ghi lại: `viewer.saveNote` **từ chối im lặng** (toast "Nhập tên để Mood biết ai dặn nhé", `return false`, UI optimistic rồi rollback) nếu chưa có `mood_client_name`; ghi chú seed phải cùng `client_identifier` với `mood_client_id` trong localStorage thì viewer mới coi là "của mình". 2 lần verify đầu fail vì 2 bẫy này, không phải code.
- User hỏi "M2 bấm icon là lọc luôn hay sao" rồi "trace kĩ đã, hướng nào thật sự tối ưu" → đo thật production + DB thay vì đoán:
  - Ảnh khách note **rải khắp album, không trùng ảnh đã chọn**: "Huyền – Vinh" 585 ảnh/126 note → 11 note trong 100 ảnh đầu, 31 trong 200, median #358; chỉ 1/126 vừa note vừa chọn. Lọc client-side trên ảnh đã tải (như tab ĐÃ CHỌN) sẽ hiện 11/126 (4G) hoặc 2/126 (2G) khi mở link.
  - **Bug có sẵn, chưa ai biết:** tab ĐÃ CHỌN lọc thiếu trên album lớn — "Xuân Phúc – Ngọc Huy" 517 ảnh/70 chọn (70 `file_group` riêng, 5/70 trong 200 ảnh đầu) → tab hiện **40 tile, 3 request trong 1s rồi đứng yên 25s**. Chuỗi AUTO-TRIGGER `use-masonry-grid.ts:145` kẹt; nghi `loadMoreServerImages` bỏ qua khi `isValidating`. "Lệ – Vũ" 526/160 hiện đủ chỉ vì tải kịp.
  - 87 album share, 1 client/album, note tối đa 126 ảnh, 1 dòng ảnh ≈ 621 byte → lấy cả danh sách noted ≈ 78KB, rẻ.
- **Quyết định (user duyệt "ok theo đề xuất bạn"): phương án B** — server action `getPublicNotedImages` (embed `gallery_comments!inner`, 1 query, không `.in` → né bẫy header 16KB), SWR lazy khi vào tab, tắt load-more server trong tab, chip 💬 bấm = tab GHI CHÚ. Edge case bắt buộc: `handleToggleStar` tra `images.find()` → ảnh noted ngoài trang đã tải sẽ no-op nếu không tra thêm `notedImages`; kẹp `viewerIndex` lúc render khi xoá ghi chú làm ảnh rời tập. Loại C (tham số RPC — cần migration) và D (sửa chuỗi auto-load rồi tải cả album — đụng hook dùng chung).
- Spec (locks `gallery-reaction-actions.ts` + `public-gallery-client.tsx`) có code trước/sau từng chỗ, AC4 (✓/❤️ trên ảnh ngoài trang đã tải, gallery E2E 172 ảnh + note ở #150), AC6 (probe tab ĐÃ CHỌN trước/sau phải y hệt) — kết quả thực thi ở dòng đầu mục này + spec §6.
- **Đề xuất M3 riêng (chưa làm):** tab ĐÃ CHỌN dùng `getPublicSelectedImages` (đã fetch toàn bộ ảnh chọn cho nút Tải, chỉ 3 cột) mở rộng `IMAGE_COLS` — sửa bug 40/70 không tốn request thêm.

## 2026-08-25 (tiếp #3) — gallery khách: header thiếu số ảnh có ghi chú — MERGED
- User báo (ảnh chụp header album share `🖼 115 · ✓ 0 · ❤️ 0`): không có icon báo số ảnh khách đã ghi chú — khách note xong không nhớ đã note bao nhiêu tấm.
- **T-20260825-gallery-header-note-count MERGED** (`78a4fb5`, Claude fallback — user duyệt "triển khai đi bạn"; giao Codex CLI 0.144.5 trước nhưng lỗi credential `404 No active credentials for provider: codex` tái hiện 2/2 dù `setup --json` báo ready → đường lùi coder-subagent theo AGENT_RULES §2). +18/−4 dòng đúng 1 file. Verify: eslint 0 (yếu — file có `/* eslint-disable */` sẵn), tsc 0, build exit 0, Playwright 14/14 PASS trên `next start`: gallery thật "Huyền - Vinh" 💬126 = `count(DISTINCT image_id)` @375/768/1024 + `?mode=view`; gallery E2E tạm: ghi chú ảnh **chưa chọn** 0→1→0 không reload, thanh đáy giữ đúng nghĩa cũ, tiêu đề 62 ký tự cắt "…" @375 không tràn. Dữ liệu test đã xoá sạch. **Production** (`stu.moodwedding.com`, Vercel deploy xong sau 191s): chạy lại bộ chỉ-đọc 7/7 PASS — 💬126 @375/768/1024 + view-only, không tràn ngang. Chi tiết + ảnh chụp: spec §6.
- Trace: `notedCount` (đã chọn ∩ có ghi chú) đã tính sẵn ở `public-gallery-client.tsx:150` nhưng chỉ hiện ở chip thanh đáy `SelectionSummary` (ẩn khi 0, ẩn ở view-only) — header (dòng 365-370) chỉ có 3 số. Ghi chú **không** bắt buộc ảnh đã chọn (nút ghi chú trong viewer `image-viewer.tsx:929` không gate `is_selected`) → số đúng cho header là **số ảnh có ≥1 ghi chú trên cả gallery** = đếm key `commentsPerImage` (map cả gallery từ server, không phụ thuộc trang đã cuộn; `handleSaveNote` đã mutate đúng key nên tự cập nhật).
- Thiết kế đã chốt: memo `notedImageCount` = số ảnh có ≥1 ghi chú trên **cả gallery** (đếm key `commentsPerImage`, không query thêm) + chip thứ 4 `MessageSquare` (đúng icon tile badge + chip đáy; KHÔNG dùng `MessageCircle` của admin stats) + `min-w-0`/`shrink-0`/`gap-3 md:gap-4` để 4 chip không tràn @375. Hiện cả khi 0 và cả ở view-only (nhất quán với ✓/❤️ và tile badge). M2 (tab GHI CHÚ) → đã tách thành `T-20260825-gallery-noted-tab`, xem mục (tiếp #4) ở trên — phương án "cùng file ~20 dòng" bị loại sau khi đo production.
- Nợ có sẵn, ghi nhận không sửa: `getGalleryComments` select 1 phát không `selectAllRows` → trần 1000 dòng/gallery (gallery thật nhiều ghi chú nhất 148) — cùng pattern T-20260808 đã vá cho reaction.

## 2026-08-25 (tiếp #2) — finance/*: bug tự động bật lại `/finance` — ĐÃ XỬ LÝ bằng watchdog (hướng 3, không ngăn nguyên nhân)
- Sau 2 hướng "ngăn nguyên nhân" không hiệu quả (mục dưới), user hỏi thẳng: hướng nào **thật sự phù hợp với quy mô thật của Mood Studio** (công cụ nội bộ, ít người dùng đồng thời, không phải SaaS traffic cao) thay vì tiếp tục đoán kỹ thuật thuần túy. Phân tích: bug xảy ra ngay cả với 1 người dùng (không phải vấn đề tải cao nhiều người), gốc rễ nằm 1 phần ngoài code ứng dụng (Next.js/Vercel), và refactor gộp 17 hook `useSWR` (hướng "sửa tận gốc") đi ngược ràng buộc dự án đã chốt ("không mở lại đợt perf diện rộng"). Kết luận: đúng hướng cho quy mô này là **phát hiện + tự sửa triệu chứng**, không cố thắng lỗi tầng hạ tầng.
- **T-20260825-finance-nav-guard MERGED** (`652caf6`, Claude fallback, user duyệt "viết spec rồi triển khai code bám chi tiết"). `FinanceNavGuard` (mới, mount trong `AppShell` cạnh `FinanceRealtimeRefresh`): theo dõi mỗi lần pathname trong `/finance/*` đổi — nếu nhảy LÊN route cha mà không có thao tác thật nào của người dùng (click/phím/back) trong 800ms trước đó, tự `router.replace()` quay lại route trước, không toast, giới hạn tối đa 2 lần tự sửa liên tiếp để tránh vòng lặp.
- **Verify thật trên production, đo bằng đúng kịch bản đã dùng cho cả 3 lần đo trước:**
  - Điều hướng hợp lệ vẫn hoạt động đúng (click thật "Tài chính"/"Công nợ KH" qua lại — không bị watchdog chặn nhầm).
  - Đo lại tỉ lệ URL sai tồn tại đủ lâu để nhận ra, 15 lần: **0/15 (0%)** — so với gốc 2/8 (25%), Fix 1 (staleTimes) 7/15 (47%, tệ hơn, đã lùi), Fix 2 (AppShell) 4/15 (27%, không đổi rõ).
- **Lưu ý trung thực:** đây là giảm triệu chứng, không phải xóa sổ nguyên nhân gốc (vẫn nằm 1 phần ở tầng Next.js/Vercel) — 0/15 không phải bằng chứng toán học tuyệt đối cho "không bao giờ xảy ra nữa," nhưng cơ chế tự sửa gần như tức thời (trong 1 chu kỳ render, dưới ngưỡng người dùng nhận ra) nên về mặt trải nghiệm thực tế coi như đã giải quyết.

## 2026-08-25 — finance/*: bug tự động bật lại `/finance` sau vài giây — CHƯA GIẢI QUYẾT DỨT ĐIỂM, cần quyết định hướng tiếp
- User báo bấm vào `/finance/debts` (qua link "Công nợ KH" trên `/finance`) thì 5-10s sau tự động bật lại `/finance`, kèm toast lỗi "An unexpected response was received from the server."
- **Bằng chứng gốc rễ đã xác nhận chắc chắn** (đọc trực tiếp nội dung JSON response + header `x-matched-path` của Vercel): response của 1 Server Action call bị **trộn** giữa 2 request đồng thời khác nhau — request gửi tới `/finance/debts` nhận nhầm dữ liệu của `/finance` và ngược lại. Đây là lỗi tầng Next.js 16 / Vercel dưới tải đồng thời cao, không phải lỗi dữ liệu ứng dụng.
- **Đã thử 2 hướng fix, đo bằng 15 lần thử thật/đợt trên production, cả 2 đều KHÔNG giải quyết dứt điểm:**
  1. Rút `next.config.ts`'s `experimental.staleTimes.dynamic` 180→30 (giả thuyết: trang `/finance` — có 17 hook `useSWR` độc lập — bị giữ "nóng" quá lâu sau khi rời trang) → đo được **7/15 (47%) — TỆ HƠN** baseline 2/8 (25%) → đã **lùi lại 180**.
  2. Chuyển `<FinanceRealtimeRefresh/>` từ `app/(protected)/finance/layout.tsx` (Server Component, re-render mỗi lần điều hướng vì trang con `force-dynamic`, gây remount + kênh realtime đóng/mở lại — có bằng chứng WebSocket log rõ ràng) sang `AppShell` (client component mount đúng 1 lần, gate bằng `pathname.startsWith("/finance")`) → đo được **4/15 (27%) — không khác biệt rõ so với baseline gốc**.
- Vẫn giữ 1 fix độc lập hợp lý: chặn bão retry cho đúng loại lỗi này trong `swrConfig` (`lib/swr.ts`, choke point qua `SWRProvider`) — không có bằng chứng làm xấu thêm dù chưa chứng minh được giúp ích rõ.
- **Nghi ngờ hiện tại (chưa xác nhận):** cả 2 hướng đã thử đều nhắm vào "giảm hoạt động của trang vừa rời" — nhưng số đo không đổi gợi ý nguyên nhân chính có thể là **bản thân MỖI trang** khi tải lần đầu đã tự bắn ra 13-17 request Server Action đồng thời, đủ để thỉnh thoảng kích hoạt lỗi trộn response, không cần trang trước đó còn hoạt động. Sửa đúng hướng này cần gộp bớt số hook `useSWR` độc lập trên các trang tài chính — phạm vi lớn hơn nhiều, không còn "surgical".
- **T-20260825-finance-nav-redirect-bug: status = `blocked`** — đã báo đầy đủ, trung thực (kể cả phần thất bại) cho user, đang chờ quyết định: đầu tư refactor lớn hơn (gộp hook), thử hướng khác (watchdog client tự phát hiện+sửa URL sai), hay tạm chấp nhận tỉ lệ ~25% và quay lại nếu user gặp lại nhiều.
- Xem đầy đủ 3 vòng đo + toàn bộ evidence trace trong `agent/HANDOFFS/T-20260825-finance-nav-redirect-bug.spec.md`.

## 2026-08-24 (tiếp #5) — finance/expenses: vá 2 phiếu tự động sót nhãn khóa + 2 điểm đếm trùng chi phí
- User yêu cầu trace nghiệp vụ + **sơ đồ quan hệ dữ liệu** (ERD, không chỉ trace chữ) cho `/finance/expenses` — xem artifact "Bản Đồ Phiếu Chi". Phát hiện: `expenses` có 4 khóa ngoại với 4 số phận khác nhau thật sự (`debt_id` 0/39 dùng — wired đúng nhưng `debts` chưa từng có dữ liệu thật; `work_task_id` 10/39 khớp đúng 10 vendor task hoàn thành; `printing_order_id` 29/39 nhưng toàn bộ là dữ liệu cũ trước ADR-014, nay chỉ còn dùng cho hoàn tiền hủy đơn; `contract_id`/`category_id` 39/39). 7 sự kiện nghiệp vụ thật sinh phiếu chi, phần lớn tự động (không phải admin gõ tay).
- **Lỗ hổng thật tìm được:** cơ chế khóa "phiếu tự động không sửa/xóa được" chỉ nhận nhãn `[Auto-` — 2/7 loại phiếu tự động (trả nợ phải trả, hoàn tiền hủy đơn in) bị sót nhãn này, sửa/xóa được tự do trên `/finance/expenses` dù đứng sau là số dư nợ/đơn in thật.
- **Rủi ro đếm trùng, liên quan trực tiếp code vừa ship hôm nay:** `buildCloseSnapshot()` (mở rộng cho Khấu hao/P&L cùng ngày) chưa loại `[Auto-Fixed]` khỏi `operatingOutflow` — đếm trùng với `fixedCost` nếu dùng "Tạo chi phí cố định tự động" rồi khóa sổ cùng 1 tháng (hiện 0 phiếu Auto-Fixed thật nên chưa phát tác). Cùng dạng lỗi (thiếu filter `vendor_id IS NULL`) tồn tại ở đường dự phòng `getContractProfitReportFallback` — dormant vì RPC chính đang sống bình thường.
- **T-20260824-expenses-guard-fixes MERGED** (`5106732`, Claude fallback, user duyệt "viết spec tối ưu rồi triển khai bám kĩ spec"). 4 fix: gắn `[Auto-Debt]`/`[Auto-Refund]` cho 2 loại phiếu bị sót; loại `[Auto-Fixed]` khỏi `operatingOutflow`; thêm filter `vendor_id IS NULL` vào đường dự phòng báo cáo lợi nhuận — cả 4 đều là thêm 1 điều kiện lọc/tiền tố vào code có sẵn, không đổi kiến trúc.
- **Cố tình không làm** (ghi rõ lý do trong spec §0): `contract-profit.ts` (dead code, 0 caller); thêm ô chọn hợp đồng/đính ảnh vào form tạo phiếu chi (quyết định sản phẩm); tách vai trò duyệt riêng (chưa có tín hiệu nhu cầu); lỗ hổng tương tự phía `receipts` (trả nợ phải thu) — cơ chế khóa khác hẳn, cần điều tra riêng.
- Verify: eslint 0 lỗi, build exit 0, render thật cả local lẫn **production** (seed E2E admin, dữ liệu tạm rồi xóa sạch) xác nhận Fix 1 (nhãn đúng + khóa Sửa) và Fix 3 (Dòng tiền ròng đúng 0đ, không đếm trùng 2 triệu test). Fix 2/4 verify bằng code review (thay đổi chuỗi xác định, khớp đúng pattern đã verify thật).
- **Phát hiện phụ, ngoài phạm vi, ghi lại cho task sau:** `debt-payment-modal.tsx` — ô "Số tiền thanh toán" không tự điền sẵn khi mở modal (dùng sai pattern `useState(() => {...})` như effect, chỉ chạy 1 lần lúc mount, không chạy lại mỗi lần chọn công nợ khác) — admin phải tự bấm "Tối đa" hoặc gõ tay.

## 2026-08-24 (tiếp #4) — finance/closes: nối "Khấu hao" + "Chốt P&L" vào số liệu thật, thêm hủy kỳ tạo nhầm
- User yêu cầu đào sâu riêng phần "khóa sổ" sau khi thấy báo cáo trace `/finance/receipts` chỉ lướt qua nó. Trace sâu (agent + verify tay từng RPC) phát hiện: `advance_close_task` là **1 máy trạng thái thuần túy** — 7/8 bước "chốt sổ" chỉ đổi status, không đọc/tính số liệu nào. Đặc biệt: "Khấu hao" (bước 6) và "Chốt P&L" (bước 7) **trùng tên với 2 tính năng có thật, tính đúng, đang chạy ở nơi khác trong app** (`/finance/investments` tính khấu hao đường thẳng thật; `/finance/reports` có P&L thật) nhưng chưa hề được nối vào đây. Cũng phát hiện: không có cách hủy 1 kỳ tạo nhầm tháng (`UNIQUE(period)`, không có nút xóa) — kẹt vĩnh viễn nếu gõ sai; audit log của việc đổi bước thiếu `recordId`/`performedBy`.
- **T-20260824-finance-closes-real-numbers MERGED** (`985c50a`, Claude fallback, user duyệt "viết spec rồi tiến hành triển khai theo đề xuất bám spec"). Mở rộng `buildCloseSnapshot()` thêm `depreciationCost` (tái dùng đúng công thức `investmentBookValue()`, tính theo mốc cuối kỳ đang chốt) và `netProfit` (`netCashflow − depreciationCost` — tách bạch dòng tiền mặt thật với lợi nhuận kế toán có trừ khấu hao phi tiền mặt). Hiển thị cả 2 số ở dòng bước 6/7 và sidebar "Snapshot SSOT". Thêm `cancelMonthlyClose()` + nút Hủy (dùng `ConfirmDialog` có sẵn) cho kỳ chưa khóa. Vá audit log thiếu `recordId`/`performedBy`.
- **Cố tình KHÔNG làm** (ghi rõ lý do trong spec §0, tránh lặp điều tra): tách vai trò kế toán riêng — quyết định sản phẩm, chưa có tín hiệu nhu cầu thật (0 kỳ đã khóa); bước 1/2/3/5 (kiểm kê quỹ, đối soát ngân hàng, công nợ, thanh toán đối tác) — không có tính năng có sẵn để nối vào, xây mới hoàn toàn ngoài phạm vi surgical fix.
- Verify: eslint 0 lỗi, build exit 0, render thật cả local lẫn **production** (seed E2E admin + chèn tạm 1 dòng `investments` test qua service-role: mua 36 triệu/36 tháng/salvage 0) — xác nhận đúng công thức (Khấu hao 1.000.000đ, P&L −1.000.000đ cho kỳ test rỗng), nút Hủy kỳ hoạt động đúng (xóa sạch kể cả 8 task nhờ `ON DELETE CASCADE`). Đã dọn sạch dữ liệu test cả local lẫn production.

## 2026-08-24 (tiếp #3) — finance/receipts: bug "Cọc" phân loại sai đang sống + nút Sửa vỡ trên phiếu bán vật tư
- User hỏi "trace `/finance/receipts` đã tối ưu chưa" — trace + render thật phát hiện **1 bug tiền thật đang sống** (không phải nợ dữ liệu cũ): RPC `finance_receipt_documents` phân loại "Cọc hợp đồng" bằng `lower(payment_stage) LIKE '%coc%'` (ASCII), nhưng `payment_stage` luôn được ghi bằng nhãn tiếng Việt có dấu (`'Cọc'`) qua `payment_stage_display_label_v2()` — không khớp pattern vì ký tự `ọ` có dấu. Mọi khoản cọc mới thu (không chỉ dữ liệu cũ) đều bị phân sai thành "Thu hợp đồng". Đo lúc phát hiện: `contract_deposit=1/49`, đúng ra phải `18/49` (17 khoản Cọc thật, tổng 21.8 triệu đ, cộng 1 khoản `dat_coc` cũ).
- **T-20260824-receipts-trace-fixes MERGED** (`eddd2bb`, Claude fallback, user duyệt "render thật trước, rồi viết spec sửa hết"). Fix bằng cách tái dùng `payment_stage_key_v2()` (hàm chuẩn hoá dấu tiếng Việt đã có sẵn, chính là hàm ghi dùng trong `process_contract_payment_v2`) thay vì tự vá `LIKE` pattern — đảm bảo đọc/ghi luôn đồng bộ. Migration `20260824140000_fix_receipt_documents_deposit_classification.sql` đã apply production, verify lại bằng RPC: đúng 18/49.
- **Bug 2 cùng task:** nút "Sửa" trên phiếu `receipt_type=sale_receipt` không bị khoá ở UI dù server luôn chặn cứng (đã có sẵn, không đổi) — bấm Sửa mở modal "Loại phiếu" rỗng, bấm Cập nhật bị chặn ngay ở client validation, không có đường đi tiếp. Thêm check `receipt_type === "sale_receipt"` vào điều kiện khoá nút Sửa (desktop table + mobile swipe card).
- 2 fix nhỏ cùng task: dòng "ID:" ở trang/modal chi tiết lộ tiền tố `payment:` (dùng nhầm `receipt.id` thô thay vì `rawId` đã strip, biến có sẵn ngay phía trên) — sửa cả 2 chỗ; `/finance/receipts` không SSR `bankInfo` (trang chi tiết đã làm) gây nháy "chưa cấu hình QR" đầu trang — thêm `getStudioInfo()` vào SSR fetch.
- Verify: eslint 0 lỗi, build exit 0, render thật cả local lẫn **production** (seed E2E admin rồi xóa) xác nhận cả 4 fix sống đúng — tab "Cọc hợp đồng" tháng 5/2026 từ 1/1 lên đúng 5/5, nút Sửa phiếu bán vật tư bị khoá đúng, ID leak hết. Một nghi vấn bug khác (tab "Bán vật tư") đã điều tra và **retract** — là race condition trong script test, không phải bug thật (verify sạch bằng RPC trực tiếp).
- **Đã trace nhưng chưa sửa, ghi lại cho task sau (xem spec mục 1):** "Người lập" trống trên phiếu in (cần join `employees` mới, chưa có pattern này ở đâu trong finance actions); `contract_adjustment` thiếu trong dropdown tạo/sửa phiếu (hiện không có đường thực tế tạo được, cần quyết định sản phẩm trước); JS fallback union trong `fetchReceipts` cắt dữ liệu ở mốc 1000 dòng/bảng trước khi lọc tháng (dormant — RPC đang sống, dữ liệu còn nhỏ).

## 2026-08-24 (tiếp #2) — Thanh toán lab: điểm vào trực tiếp trên đơn
- **T-20260824-lab-payment-entry-points MERGED** (`25f2856`, Claude fallback theo yêu cầu user: "ok duyệt, bạn tiến hành viết spec đầy đủ rồi tiến hành bước tiếp"). User báo bằng 3 ảnh: không có UI thanh toán công nợ lab trực tiếp từ dòng/thẻ đơn (phải vào "Sửa" mới tới `LabPaymentModal`), và ở đó chế độ "Chọn thủ công" hiện cả 26 đơn chưa trả, không ưu tiên đúng đơn admin đang xem — "loạn xà ngầu hoàn toàn không tối ưu".
- **Thiết kế:** công nợ Lab vẫn là khái niệm cấp LAB (không đổi data model), nhưng 2 điểm vào ứng 2 ý định khác nhau. Từ 1 đơn cụ thể (nút "Thanh toán" mới, cùng cấp "Sửa", trên `OrderRow`/`PrintingCard`) → modal tự bật "Chọn thủ công", tự tick đúng đơn, tự điền `amount = remainingAmount` thật. Từ lab (`/printing/labs`, không đổi) → vẫn FIFO mặc định.
- Diff: `lab-payment-modal.tsx` (+prop `focusOrderId`), `printing-card.tsx`/`printing-table.tsx`/`printing-group-drawer.tsx` (+prop `onPayLab` + nút), `printing-list-page.tsx` (state + dynamic-import modal), `printing-detail-drawer.tsx` (+1 dòng). Chi tiết: `agent/HANDOFFS/T-20260824-lab-payment-entry-points.spec.md`.
- Verify: eslint 0 lỗi, build exit 0, render thật (local + **production** `stu.moodwedding.com`, Playwright seed E2E admin rồi xóa) xác nhận cả 2 hành vi đúng — không tạo thanh toán thật lúc verify.
- **Còn mở, việc của user:** dùng nút "Thanh toán" mới để bắt đầu ghi nhận trả nợ thật cho lab Hồng Bảo (~8.15 triệu đ / 26 đơn, `total_paid=0` tính tới lúc merge).

## 2026-08-24 (tiếp) — In ấn: bỏ đặt cọc/giao khách/kho (ADR-014)
- **T-20260824-printing-workflow-redesign MERGED** (`0f9a3cb`, Claude fallback theo yêu cầu user). Nghiệp vụ đúng xác nhận trực tiếp: in ấn = Mood ⇄ Lab thuần tuý, không cọc, không kho vật tư, `da_in` = lab đã in xong nhưng hình còn ở lab, `hoan_thanh` = Mood đã nhận về. Trạng thái sản xuất rút còn 4 bước (`cho_xu_ly→dang_in→da_in→hoan_thanh`); công nợ Lab tách hẳn thành trục B độc lập (`record_lab_payment_atomic`, không đổi).
- **Migration production đã áp**: gộp 3 đơn `da_nhan` + 2 đơn `dat_coc` (0 tiền thật) vào `hoan_thanh`, thêm CHECK constraint, viết lại `printing_stats()`. Sửa luôn bug lệch từ vựng `payment_status` (cùng root cause) — KPI "Chưa thanh toán" từng hiện 0đ nay đúng.
- **Follow-up cùng ngày** (`51988da`): dropdown trạng thái đơn in không lọc theo `VALID_TRANSITIONS` (user phát hiện qua ảnh chụp — cho chọn cả nhảy cóc lẫn 2 giá trị legacy `da_nhan`/`da_huy`). Thêm `PRINTING_VALID_TRANSITIONS` (`types/printing-constants.ts`) làm nguồn chân lý dùng chung server+client, lọc dropdown còn đúng `{hiện tại} ∪ bước hợp lệ tiếp theo`. Verify render thật trên **production** (`stu.moodwedding.com`, không phải local) sau deploy.
- **Công nợ Lab Hồng Bảo — XÁC NHẬN LÀ NỢ THẬT** (user 2026-08-24: "hợp đồng thật mình vẫn chưa qua menu /printing để update"). Số đo lúc merge 7.916.400đ/25 đơn; số mới nhất **8.151.400đ / 26 đơn, `total_paid=0`** (tăng vì hoạt động thật tiếp diễn) — chưa từng ghi nhận qua `record_lab_payment_atomic` lần nào. Việc tiếp theo (nếu user muốn) là bắt đầu dùng nút "Thanh toán lab" ở `/printing` hoặc `/printing/labs` để ghi nhận trả nợ thật.

## 2026-08-24 — Sentry/V1 + form hợp đồng
- **Sentry issue `142429860` (TypeError "waiting", l.fn undefined @ /login) KHÔNG phải bug V2.** Event đến từ `admin.moodwedding.com` = **app V1 cũ vẫn đang chạy** (build < 2026-04-24, Supabase riêng `dtrrnkybahstrvgsxcgp`), do bot Playwright (Firefox 128) chặn SW → workbox-window `register()` đọc `this._registration.waiting`. V1 và V2 dùng **chung 1 DSN key** → đã đổi Sentry project `allowedDomains` `["*"]` → `["stu.moodwedding.com","localhost"]` (probe: V1 tunnel 403 Cors, V2 200). Hệ quả: preview `*.vercel.app` không còn báo lỗi client. Còn mở: hạ V1 trên Vercel (CLI chưa login), quyết số phận Supabase V1, ignore issue trên UI (token thiếu quyền).
- **T-20260824-customer-search-contrast MERGED** (`7b67da5`, Claude fallback): lớp SSOT `.input-elevated` (forms.css) cho ô search inline trên nền trang; placeholder "Tìm hoặc tạo mới..." (đo width @375). Verify: eslint, build, render thật `next start` @375/768/1024 + focus.

## E2E (2026-08-08) — suite chromium XANH 100%
- Toàn bộ spec chromium pass, KỂ CẢ `printing-ui-tablet` (workers=2 full, file perf/DB-nặng workers=1).
- `NEXT_PUBLIC_RPC_V3` **ĐÃ BẬT cả local lẫn build prod**.
- ✅ **Bug v3 print_orders ĐÃ FIX + APPLIED:** migration
  `20260808130000_fix_contract_detail_v3_print_orders_fields.sql` đã chạy trên DB —
  block In ấn prod hết hiện "Chưa có SP/Rỗng/Thiếu file" sai (verify DB + E2E + screenshot).
- Known-limitation: 4 test mở detail của `contracts-tablet-ipad` treo click row trên
  WebKit emulation (project "iPad A16 Landscape") — chưa mổ, chromium pass đủ.

## Hiệu năng — phần lớn ĐÃ SHIP (qua commit, không nằm trong PLAN cũ)
- SSR-first bootstrap thay client-waterfall (gallery, printing 1-call, dashboard TTL).
- Intent-based prefetch + tắt speculative warmup (`90aea43`).
- RPC v3 contract-detail đã code — **sau flag `NEXT_PUBLIC_RPC_V3`** (`app/actions/contract-queries.ts:527`). ⚠️ Cần xác nhận prod đã bật chưa.
- Region pin **sin1** (`3a1bf52`) — RTT server→DB giảm.
- Optimistic zero-latency phủ sâu contracts/finance; blurhash ngoài critical path.
- Bundle gate `perf:chunks`; React Compiler on; image WebP/AVIF (`next.config.ts`).

## Còn mở (nhìn thấy trong code)
- **`force-dynamic` trên 41 page** — nav vẫn server-render mỗi lần vào (mới giảm đau bằng prefetch). Gỡ tận gốc = PPR/`cacheComponents` (cần DECISION).
- RPC v3 còn sau flag — nếu prod chưa bật = win rẻ đã code sẵn.

## Hướng đi hiện tại của repo
- ~3 tuần gần đây commit đã rời perf sang **feature**: Moodie (voice/agent runtime), contract printable layout, multi-day event schedule. Tức perf đã tới mức "đủ tốt".

## Việc perf khuyến nghị (nếu tiếp) — theo thứ tự
1. Đo 1 lượt: `perf:audit` / `perf:contract-detail` + Vercel Speed Insights → số đẹp thì tuyên bố xong.
2. Check prod `NEXT_PUBLIC_RPC_V3`.
3. Chỉ khi đo thấy nav còn chậm thật → PPR cho trang force-dynamic (mở DECISION).

## In-flight (kiểm tra trước khi tạo task đụng cùng vùng)
- Worktree `.worktrees/contract` @ branch `codex/contract-optimization` — Codex, contract module. Xác nhận còn sống/đã merge trước khi giao task chạm `app/**contract**`, `components/contracts/**`.

## Nợ kỹ thuật đã lộ (2026-07-14, qua lần chạy CI đầu)
- **`npm run lint` full đang ĐỎ: 27 errors + 24 warnings.** Phần lớn là SSOT (native `<button>`/`<input>` thay vì `@/components/ui`, arbitrary Tailwind `text-[...]`/`shadow-[...]`), tập trung ở `components/contracts/**`, `components/moodie/**`, `components/gallery/**`.
- ⚠️ Trong đó có **1 lỗi thật (không chỉ style):** `components/gallery/image-viewer.tsx:267` — `useCallback` gọi **có điều kiện** (`react-hooks/rules-of-hooks`) → nguy cơ "Rendered more hooks". Nên ưu tiên xử lý (task Codex riêng).
- CI hiện **chỉ lint file thay đổi** để nợ này không chặn PR khác. Dọn nợ = 1 task riêng (Codex), không làm gộp.

## Con số cần cập nhật (chưa có tại thời điểm này)
- p75 LCP/INP production (Speed Insights) — **CHƯA đo** trong phiên hiện tại.
