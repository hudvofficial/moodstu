# DB-CHANGELOG — sổ thay đổi lược đồ production

**Chuẩn S2 (chương trình tối ưu, GĐ1).** Từ 02/09/2026, MỌI thay đổi trên DB production (CREATE OR REPLACE · DROP · ALTER · CREATE POLICY · migration áp qua `migrate-direct.mjs`) phải có một dòng ở đây, ghi TRƯỚC KHI áp. Không có dòng = không được áp.

Mẫu dòng:
`| ngày | đối tượng | loại | file migration / spec | dump bản sống ở | revert ở | người duyệt |`

## Baseline 02/09/2026

| Ngày | Đối tượng | Loại | Nguồn | Dump | Revert | Duyệt |
|---|---|---|---|---|---|---|
| 2026-09-02 | *toàn bộ catalog* | BASELINE | `agent/inventory/18-diff-db-repo.md` + `vault/30-du-lieu/` (than-ham · rls-va-quyen · luoc-do-*) | sinh tự động, tái tạo bằng `npm run vault:db-truth` | — | — |

Trạng thái baseline: DB 149 hàm · 217 policy · 93 bảng · 81 trigger · 16 enum. Sổ Supabase dừng ở `20260621100000` (113 bản ghi); ~90 migration sau đó áp tay không sổ. Từ dòng này trở đi, sổ này là nguồn ghi nhận duy nhất.

## Thay đổi

| Ngày | Loại | Việc | Backup trước | Đường lùi |
|---|---|---|---|---|
| 2026-09-05 | dữ liệu (dọn rác test) | Xoá 6 `auth.users` `@test.local` tồn từ 08–28/08 (`tmp-verify-gallery`, `perf-probe-mtbpl9z5` — **admin active**, `e2e-m1/m3/pdf×2` mồ côi) + 2 dòng `employees` gắn với chúng (`PERF-bpl9z5`, `NV-7F39F7`). Không HĐ/task/lịch liên quan (đã kiểm). Qua API service-role, không SQL tay. Theo lệnh chủ "data test nhớ dọn" (#8). **Phát hiện thêm:** 4 user mồ côi bị FK chặn vì còn **4 phiếu chi E2E sống** (`expenses` 19–24/08: "E2E PDF" 100k ×2, "E2E trả lab" 300k, "E2E trả ekip" 1,2tr = **1.700.000đ giả trong sổ T8**, rác sự cố 28/08) + 2 `expense_allocations` trỏ đơn in đã xoá → xoá 2 phân bổ + 4 phiếu chi + 4 user. Đối soát chờ: `finance_pnl_by_month(2026)` T8 `cash_out` 19.796.400 → **18.096.400** (khớp CURRENT_STATE 26/08) — **đã đối soát sau xoá: 18.096.400 ✓**. Dọn thêm 1 `labs`, 2 `vendors`, 1 `inventory_items` tên E2E (không đơn/task/giao dịch). Quét 10 bảng: 0 dòng E2E. | `mood_20260905_0200.dump` OK | không cần — tài khoản test, tạo lại được |
| 2026-09-07 | hàm (CREATE OR REPLACE ×2) — **CHUẨN BỊ, chưa áp** | **#12 R2:** `finance_period_ledger` + `contract_financials` loại phiếu chi danh mục `contract_refund/refund/hoan_tien` khỏi `cost_direct`/`direct_cost` (hoàn cọc = tiền ra, không phải chi phí; HĐ `da_huy` đã bị loại khỏi doanh thu). Chữ ký giữ nguyên. File `supabase/migrations/20260907090000_r2_ledger_hoan_tien_khong_phai_chi_phi.sql`, sinh từ thân hàm sống. Hôm nay 0 phiếu hoàn → số không đổi; vá trước khi #13 mở van huỷ HĐ. | `mood_20260907_0212.dump` OK | `agent/HANDOFFS/T-20260907-r2-ledger-hoan-tien.revert.sql` (2 thân hàm sống) |
| 2026-09-07 | hàm (CREATE OR REPLACE ×2) — **ĐÃ ÁP** | **#12 R2 ĐÃ ÁP** qua `ALLOW_PROD_WRITE=1 node scripts/migrate-direct.mjs 20260907090000_r2_ledger_hoan_tien_khong_phai_chi_phi.sql` (chủ "duyệt" 07/09). Diễn tập local trước: hoàn cọc 1tr → `cost_direct` 1.200.000 → 200.000, `cash_out` giữ 1.200.000, revert.sql quay về đúng. Prod trước/sau: `finance_pnl_by_month(2026)` 12 dòng · `finance_month_summary(9)` · `contract_financials` 62 HĐ **khớp 100%** (0 phiếu hoàn). EXPLAIN 9,4 → 11,6 ms. `vault:db-truth` chạy lại: 2 hàm mang R2. | `mood_20260907_0212.dump` | `revert.sql` đã thử trên local ✓ |
| 2026-09-07 | hàm (CREATE OR REPLACE ×1) — **CHUẨN BỊ, chưa áp** | **#13 R1:** `cancel_contract_cascade` ghi `printing_orders.status = 'huy_don'` (đúng CHECK `printing_orders_status_check` từ 24/08) thay `'da_huy'`; WHERE loại thêm `huy_don`. Đổi đúng 2 chữ, sinh từ thân hàm sống. Tái hiện trên local: 23514 check violation → sau fix HĐ `da_huy`, đơn `huy_don`, task `da_huy`. File `supabase/migrations/20260907100000_r1_cancel_cascade_huy_don.sql`. | `mood_20260907_0212.dump` OK | `agent/HANDOFFS/T-20260907-r1-cancel-cascade.revert.sql` |
| 2026-09-07 | hàm (CREATE OR REPLACE ×1) — **ĐÃ ÁP** | **#13 R1 ĐÃ ÁP** (chủ "duyệt" 07/09) qua `migrate-direct` cờ S3. Thân hàm sống: đơn in `huy_don` ✓ (`da_huy` còn lại chỉ ở `work_tasks`, đúng). Dữ liệu trước/sau (count HĐ + đơn in theo trạng thái) khớp 100%. Cửa thật: #14 chủ huỷ HĐ kẹt trên UI. `vault:db-truth` chạy lại. | `mood_20260907_0212.dump` | `revert.sql` (đã thử local) |
| 2026-09-10 | dữ liệu (dọn rác test) | Chủ: "data test đang rải rác trên production". Quét **370 cột text × mọi bảng** + mồ côi: còn **2 `employees` "E2E Finance" (28/08, department "Chưa phân bổ" → sweep cũ lọt, auth đã xoá)** · 1 `credit_cards` E2E-RT (0 debts) · 16 `login_attempts` (probe-anon-access/e2e) · 67 `audit_logs` mang dấu E2E (13/07–05/09; bảng append-only cho app, xoá qua service role). Xoá qua API, quét lại = 0. `realtime_signals` 100 dòng là tín hiệu hôm nay (ring buffer), không phải rác. **Vá gốc:** `tests/e2e/e2e-sweep.ts` quét thêm employees theo tên, expense_allocations→expenses, labs/vendors/inventory/credit_cards/login_attempts/audit_logs. | `mood_20260910_*.dump` | tài khoản/dòng test, tạo lại được |

| 2026-09-10 | policy (DROP 37 · CREATE 7) + grant (REVOKE ghi 9 bảng) + hàm (`calendar_month_events` thêm `p_employee_id`) — **CHUẨN BỊ, chưa áp** | **#24 T3 tầng RLS:** (A) 6 bảng HĐ (`contracts`, `contract_events`, `contract_checklists`, `contract_notes`, `payment_plans`, `payment_plan_allocations`) có 2 policy SELECT cộng OR — `*_authenticated_read` = `is_active_employee()` (ctv/media đọc mọi HĐ) và `*_select` scope `created_by = get_current_employee_id()` (**chết**: `created_by` là auth uid, 63/63 HĐ không khớp `employees.id`) → 1 policy `<bảng>_read` gương ma trận `types/roles.ts` (admin/manager/sale; `work_tasks` thêm việc của mình). (B) `authenticated` đang có INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER trên 9 bảng + 24 policy ghi → sale PATCH được mọi HĐ bằng anon key; app ghi 100% qua service role (`withAuth` → `createAdminClient`; 0 chỗ ghi từ trình duyệt; 0 hàm INVOKER/trigger ghi vào 9 bảng; `sync-worker` dùng service key) → REVOKE ghi + gỡ 24 policy ghi (`anon` 0 grant từ trước). (C) R8: `calendar_month_events(p_month, p_year, p_employee_id uuid DEFAULT NULL)` lọc lịch tay theo người khi truyền; ACL giữ `{postgres, service_role}`. Prod: 46 policy · 63 grant authenticated · 1 admin có đăng nhập, 3 sale + 6 ctv **chưa có auth** (lỗ chưa bị khai thác — vá trước khi cấp tài khoản). Local `mood_restore`: sale 66 HĐ + UPDATE **42501** · ctv 0 HĐ / 1 việc của mình / 1 lịch của mình · lịch ctv 3→1 · revert về 46 policy/63 grant/hàm 2 tham số ✓ → áp lại 16 policy/9 grant ✓. | `mood_20260910_0209.dump` OK (6,1 MB · 93 bảng) | `agent/HANDOFFS/T-20260910-t3-rls-matrix.revert.sql` (46 policy + 9 grant + thân hàm + ACL sống; đã thử local ✓) |

| 2026-09-10 | policy + grant + hàm — **ĐÃ ÁP** | **#24 T3 ĐÃ ÁP** (chủ "duyệt" 10/09) qua `ALLOW_PROD_WRITE=1 node scripts/migrate-direct.mjs 20260910100000_t3_rls_matrix_revoke_writes_calendar_scope.sql` → "completed successfully". Prod trước/sau: 9 bảng **46 → 16 policy** (7 `_read` + 7 `_service_role_all` + `customers_select` + `schedules_select`) · grant `authenticated` **63 → 9** (chỉ SELECT) · `anon` 0 → 0 · hàm `calendar_month_events(integer,integer,uuid)` ACL `{postgres, service_role}` giữ nguyên, thân hàm có dòng R8. Dữ liệu **khớp 100%**: contracts 65 · contract_events 220 · contract_checklists 338 · contract_notes 2 · payment_plans 123 · payment_plan_allocations 69 · work_tasks 171 · schedules 3 · customers 66. `calendar_month_events(9,2026)` = `(…,NULL)` = 83; truyền id người lạ = 82 (lọc đúng 1 lịch tay). **Probe REST thật** (anon key + JWT, 2 user tạm sale/ctv, đã xoá + `login_attempts` dọn): sale GET contracts 65 / events 220 / checklists 338 / payment_plans 123 / customers 66 / schedules 0 · sale PATCH contracts, POST contract_notes, DELETE customers, RPC calendar → **403 `42501`** ×4 · ctv GET contracts/events/customers/work_tasks = **0** · ctv PATCH work_tasks 403 → **15/15**. App: `calendar-queries.ts` truyền `p_employee_id`, `database.types.ts` Args. `vault:db-truth`: 149 hàm · **187 policy** (217 baseline → 187). | `mood_20260910_0209.dump` | `revert.sql` (đã thử local 2 lần, kể cả ACL hàm) |

| 2026-09-10 | ràng buộc (ADD CONSTRAINT CHECK ×2, NOT VALID → VALIDATE) — **CHUẨN BỊ, chưa áp** | **#26 T4:** `work_tasks_status_check` + `contract_events_status_check` = `status IS NOT NULL AND status IN ('chua_lam','dang_lam','hoan_thanh','da_huy')` (đúng kiểu TS `TaskStatus`). Prod 10/09: `work_tasks` 171 dòng (hoan_thanh 167 · dang_lam 2 · da_huy 2), `contract_events` 220 dòng kể cả xoá mềm (hoan_thanh 164 · chua_lam 14 · dang_lam 4 · da_huy 38) — **0 NULL, 0 giá trị lạ** → VALIDATE ngay trong cùng lần áp. App ghi đúng 4 giá trị (literal + `input.status: TaskStatus`); cascade ghi `da_huy`; trigger chỉ `updated_at`/realtime. Không đụng: 94 mốc `hoan_thanh` không ngày (hau_ky 56, giao_san_pham 31 → #30). Local: ghi sai `hoan_thnah`/`xong`/NULL → **23514** ×4, hợp lệ OK, revert ✓ áp lại ✓. File `supabase/migrations/20260910120000_t4_check_status_work_tasks_contract_events.sql`. | `mood_20260910_0209.dump` OK | `agent/HANDOFFS/T-20260910-t4-check-status.revert.sql` (2 DROP CONSTRAINT; trước áp 2 bảng không có CHECK trên status) |

| 2026-09-10 | ràng buộc (CHECK ×2) — **ĐÃ ÁP** | **#26 T4 ĐÃ ÁP** (chủ "duyệt" 10/09) qua `ALLOW_PROD_WRITE=1 node scripts/migrate-direct.mjs 20260910120000_t4_check_status_work_tasks_contract_events.sql`. `pg_constraint`: `work_tasks_status_check` + `contract_events_status_check` **validated**. Dữ liệu trước/sau khớp 100% (171 · 220, phân bố y nguyên). Hàng rào thật qua service role: `hoan_thnah`/`NULL`/`xong`/`NULL` → **4/4 `23514`**, dòng giữ nguyên. `verify:contracts` pass · `tsc` 0. `vault-gen-schema` + `vault:db-truth` chạy lại (lược đồ 9 file cập nhật số dòng trôi từ 02/09). Sweep rác 0. | `mood_20260910_0209.dump` | `revert.sql` (2 DROP CONSTRAINT, đã thử local) |

| 2026-09-10 | hàm ×3 mới + trigger ×2 + index ×2 + CREATE OR REPLACE ×1 — **CHUẨN BỊ, chưa áp** | **#27 T4:** `normalize_phone(text)` IMMUTABLE (chữ số; `84`/`0084` → `0`; rỗng → NULL; không sửa độ dài) · trigger `normalize_phone_before_write` BEFORE INSERT/UPDATE OF phone trên `customers` + `crm_leads` · index `idx_*_active_normalized_phone` · `convert_lead_to_customer` đổi 2 dòng (so khớp + ghi theo bản chuẩn; sổ đối chiếu dòng 89: lead `+84…` tạo khách trùng) · `customer_phone_report()` chỉ service_role. **Không backfill.** Prod 10/09: customers 66 (60 chuẩn, 6 sai độ dài), crm_leads 4, 0 `+84`, 0 khách trùng. Local: 10 mẫu chuẩn hoá đúng, trigger insert/update ✓, convert lead `+84 912…` nối vào khách `0912…` (65→65) ✓, báo cáo 6 `sdt_khong_hop_le`, revert ✓ áp lại ✓. File `supabase/migrations/20260910130000_t4_normalize_phone_customers_leads.sql`. | `mood_20260910_0209.dump` OK | `agent/HANDOFFS/T-20260910-t4-normalize-phone.revert.sql` (gỡ 7 đối tượng + thân RPC sống) |

| 2026-09-10 | hàm ×3 + trigger ×2 + index ×2 + RPC ×1 — **ĐÃ ÁP** | **#27 T4 ĐÃ ÁP** (chủ "duyệt" 10/09) qua `ALLOW_PROD_WRITE=1 node scripts/migrate-direct.mjs 20260910130000_t4_normalize_phone_customers_leads.sql`. Prod sau: trigger 2 · index 2 · hàm 3; `convert_lead_to_customer` so theo `normalize_phone`, ACL giữ `{postgres, service_role}`; `customer_phone_report` chỉ service_role. Dữ liệu **không đổi** (md5 chuỗi phone customers `aff21835`, crm_leads `3255eecf` trước = sau). Hàng rào thật: lead thử `+84 968 000 111` → `0968000111`, UPDATE `(0912) 000-222` → `0912000222`, đã xoá (4 → 4). `verify:customers`: 0 trùng · 6 SĐT sai độ dài (không backfill). App: `lib/phone.ts` + 3 action, jest 9/9, tsc 0. Vault db-truth + schema + module CRM. Sweep 0. | `mood_20260910_0209.dump` | `revert.sql` (đã thử local) |

| 2026-09-10 | dữ liệu (dọn rác test, lần 2) | Khi đọc V0 mốc 3 thấy `audit_logs` contracts CREATE/DELETE hàng trăm dòng/ngày → truy: **13.629 / 17.095 dòng (80%)** là nhật ký của HĐ/khách seed E2E đã xoá từ 02/05 tới 10/09 (`new_data`/`old_data` mang mã `E2E-…`, `description` NULL nên sweep cũ báo 0). Xoá qua service role theo ngày (lô 1000 id trên URL → 400 Bad Request), chỉ dòng khớp mẫu JSON `E2E%`; còn **3.466** dòng thật. ~90 dòng contracts mồ côi không mang dấu E2E giữ nguyên (không đoán). **Vá gốc:** `scripts/sweep-e2e-residue.sql` + `tests/e2e/e2e-sweep.ts` quét thêm `new_data/old_data ->> contract_code|customer_code|full_name|contact_name|employee_code|name`. Quét lại: 22 bảng = 0. | `mood_20260910_0209.dump` (còn đủ 13.629 dòng nếu cần soi) | rác seed, không cần |

| 2026-09-10 | hàm (CREATE OR REPLACE ×1 + ACL) — **CHUẨN BỊ, chưa áp** | **#18 T1:** `get_finance_intelligence` đọc `finance_debt_stats()` cho phải thu/phải trả thay bảng `debts` (0 dòng từ đầu → điểm công nợ luôn "Lanh manh" giả; sổ đối chiếu dòng 35). Đổi đúng 1 khối (dòng 79–91), sinh từ thân sống. Siết ACL `{postgres, anon, authenticated, service_role}` → `{postgres, service_role}` (SECURITY DEFINER trả toàn bộ số tài chính; app chỉ gọi qua withAuth). Prod hôm nay: receivable 8,5tr · payable 14,14tr. Local: stats 0/0 → 92,575tr/13,905tr (= debt_stats dump), revert ✓ áp lại ✓. Kèm C8: gỡ thẻ Health-score + Hòa vốn khỏi `/finance` và `/finance/dashboard`. File `supabase/migrations/20260910140000_t1_finance_intelligence_debt_stats.sql`. | `mood_20260910_0209.dump` OK | `agent/HANDOFFS/T-20260910-health-score-debt-stats.revert.sql` (thân + ACL sống) |

| 2026-09-11 | hàm (CREATE OR REPLACE ×1 + ACL) — **ĐÃ ÁP** | **#18 T1 ĐÃ ÁP** (chủ "duyệt" 11/09) qua `ALLOW_PROD_WRITE=1 node scripts/migrate-direct.mjs 20260910140000_t1_finance_intelligence_debt_stats.sql`. Prod trước/sau: `stats.receivables` **0 → 8.500.000**, `stats.payables` **0 → 14.140.350** (khớp `finance_debt_stats()`), `breakdown.receivables` **15 "Lanh manh" → 5 "No phai tra cao"**, `health_score` **100 → 90**, `projectedBalance` 241.848.600 → 236.208.250; thân hàm hết `FROM debts`, có `finance_debt_stats() d`; ACL `{postgres, anon, authenticated, service_role}` → **`{postgres, service_role}`** (`authenticated`/`anon` EXECUTE = false). Bảng `debts` **không đụng** (vẫn 0 dòng). App theo C8: gỡ thẻ Health-score + Hòa vốn khỏi `/finance` và `/finance/dashboard`, giữ Runway; Playwright `finance-intel-c8` **2/2**. `vault:db-truth` 152 hàm. Sweep rác 0. | `mood_20260910_0209.dump` | `revert.sql` (thân + ACL sống, đã thử local 2 vòng) |

_(dòng kế tiếp: #23 buildCloseSnapshot + finance_cashflow_timeline → ledger)_
